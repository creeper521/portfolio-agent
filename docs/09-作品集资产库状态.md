# 作品集资产库准备状态

> **状态：** Wave 2 全量公开资产已批准、本地发布并原子导入当前分支运行时；尚未部署
> **核对日期：** 2026-07-30
> **用途：** 说明本地资产盘点已经如何进入项目治理流程，以及哪些能力仍未公开。

## 1. 当前资产范围

本轮已将本地知识库中的成果整理为 68 项私有资产记录：

| 类别 | 数量 | 说明 |
|---|---:|---|
| 长期主线 | 7 | 跨多日或多阶段持续演进的成果 |
| 单体任务 | 19 | 已交付功能、本地原型、工程操作和文档任务 |
| 事件与故障 | 25 | 开发问题、线上排查、协助和观察学习 |
| 知识资产 | 17 | 技术评测、方案研究、手册和写作成果 |
| 合计 | 68 | 全部保持独立状态与贡献边界 |

每项记录均包含内容类型、完成状态、贡献类型、公开优先级、证据状态和审核状态。不确定贡献、缺少最终验收或仅属观察学习的内容不会被升级为个人交付成果。

## 2. 已生效的 Wave 1

首批选择四项资产完成脱敏、审核与发布：

1. **SQL 审计主线扩充**：补充负号输入安全、多来源选择、成功结果保留和选中目标检查；
2. **多语言图片修复**：描述后续上传不再覆盖既有语言结果的实现与顺序上传回归；
3. **测试角色重置工具**：描述环境选择、查询、二次确认、重置、复查和重新创建闭环；
4. **CodeGraph 评测**：保留工具收益、失效边界和“图谱导航 + 文本与源码精确复核”的组合决策。

SQL 项作为现有公开 Project 的 2026-07 增量；其余三项以独立 CaseStudy 进入 schema 3.0 公开 Bundle。

## 3. 历史全量公开发布（`2026-07-27.1`）

在不改变 68 项私有登记的前提下，该历史批次将全部非排除资产整理、批准并发布为 `2026-07-27.1`：

| 范围 | 数量 |
|---|---:|
| 公开资产 | 61 |
| 已在 Wave 1 公开 | 9 |
| 本轮新增公开 | 52 |
| 保持排除 | 7 |
| 运行时 Project | 7 |
| 运行时 Case | 49 |
| 运行时 Claim | 81 |
| 运行时 Evidence 摘要 | 59 |
| 运行时 QuestionPreset | 16 |

52 项新增公开资产被组织为 6 条长期主线和 46 个独立 Case，而不是压缩成少数演示项目。完成状态、个人贡献和证据强度均按原始登记保守映射；调查、协助、未解决和自我确认内容不会被改写成“独立、已验证交付”。

7 项 `EXCLUDE` 继续保持私有。原始 Evidence 不进入仓库或候选，公开候选只携带脱敏摘要，且 `rawContentPublic=false`。

## 4. 当前状态

- 全量资产结构检查通过：68 项、无重复、无缺失、无未知编号；
- 首批候选敏感信息检查通过；
- 首批候选跨文件状态和贡献一致性检查通过；
- 内容所有者已按精确候选 hash 完成人工 Approval；
- Wave 1 四项已发布并原子导入为内容版本 `2026-07-24.1`；
- 37 例真实模型比较证明策略 v2.1 的三路 false-sufficient 为 0，Hybrid 正向充分判定为 20/26；
- 全量候选结构、引用、状态映射、隐私和冻结治理基准通过；
- 89 例真实比较中，Hybrid 正向充分判定 32/38，高于 Keyword 13/38 和 Vector 17/38，三路 false-sufficient 均为 0；
- 完整发布门禁已通过：前端 206 项、后端 391 项、治理 CLI 94 个场景、真实 BGE 比较和打包应用 32 项端到端测试均成功；
- 内容所有者已批准精确候选哈希，Approval 为 `APR-4b975c5137154497ac0084575d6628d7`；
- `2026-07-27.1` 曾发布到本地发布区并原子导入；当前随包版本已由后续 `2026-07-29.1` 取代；
- 生产部署和线上验收尚未执行。

自动检查只能证明候选结构与边界符合要求，不能替代内容所有者对公开文案、证据摘要和发布版本的人工批准。

## 5. 公开运行时状态与边界

当前随包公开运行时已经更新为 schema `4.0`：

- schema 为 `4.0`，加载器仍兼容历史 schema 2.0/3.0；
- 随包公开内容包含 5 个 Project、49 个 CaseStudy、3 个 Collection、79 个 Claim、59 个 APPROVED Evidence、79 条 Claim–Evidence link、79 个 retrieval chunk 和 16 个 QuestionPreset；
- 七文件 Bundle 已包含发布期生成的 Keyword 与 Vector 索引，策略版本为 `retrieval-policy-v2.1-query-risk`；
- 后端新增 Case 列表/详情 API，聚合接口新增 Case 数据和反向证据索引；
- Case 专属 QuestionPreset 已可由 Agent 后端执行，Case 详情页到 Agent 的上下文与建议问题交接入口已实现；
- Case 列表页、详情页、规范路由、筛选分组、Project 反向入口与对应前端测试已经实现；
- 私有知识库和候选包不会被应用直接读取。

运行时内容版本为 `2026-07-29.1`。这表示内容已进入当前分支随包数据，不表示 PostgreSQL/pgvector 已经生产启用或通过线上组合检索验收。

Bundle 中的 Case 已进入后端检索、上下文、固定工具与预设执行链路，也已进入当前前端的 Case 列表页、详情页和 Agent 交接入口。尚未完成的是生产部署、线上数据验证与完整生产验收；不能把“当前分支已实现”表述成“线上已经可用”。

## 6. 已落地公共契约与后续工作

CaseStudy 公共契约已经实现并验证，覆盖：

- Case 类型、完成状态和个人贡献；
- 问题、行动、决策、验证、结果与限制；
- Claim–Evidence 关联；
- 可选父 Project；
- 后端 API 与公开 DTO；
- 前端 Case Repository 映射、列表/详情页、规范路由与 Agent 上下文交接；
- schema 迁移、兼容和失败关闭测试。

Case 分组前端模型支持长期主线、单体任务、问题处理、知识与评测四组；完整 Case Repository 映射、列表/详情页、视觉与 Agent Case 入口均已在当前代码和测试中落地。本阶段仍不宣称生产可用：后续工作是部署环境联调、线上数据验证、可观测性核验与生产验收。

## 7. 隐私边界

公开项目不会接收或展示：

- 原始日报、内部截图、代码、日志和查询语句；
- 公司、产品、人员、团队和真实业务标识；
- 内部域名、主机、地址、端口和本地路径；
- 凭据、账号、角色、活动、渠道或区域的真实标识；
- 无证据支持的调用次数、用户数量、效率百分比或长期生产影响。

允许进入后续公开审核的内容仅限于重新撰写的机制说明、个人贡献、技术决策、验证结果和明确限制。
