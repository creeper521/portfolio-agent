# Agent 项目演进日志

> 本文按日期和开发阶段记录 Agent 项目功能、重要修复、产品决策与技术选型的演进。
> 本文不记录具体代码步骤、测试过程或 Git 提交信息；当前能力边界仍以《当前实现状态》和《文档状态索引》为准。

## 状态说明

- **提出**：形成想法或待确认方向。
- **选定**：完成方案选择，但尚未实现。
- **已实现**：能力已经进入当前项目。
- **受限启用**：已经实现，但受审批、配置或环境限制。
- **默认关闭**：已经实现，但默认运行路径不启用。
- **已取代**：曾经采用，后来被新方案替换。
- **未部署**：本地能力已经完成，尚未进入生产环境。

## 2026-07-14

### 阶段一：确定公开实习作品集 Agent 的产品边界

- 项目被定义为面向公开访问者的实习作品集 Agent，而不是连接私人知识库的个人 Copilot。
- 运行时数据源限定为经过审核、脱敏和批准的公开快照；私人 Obsidian、原始日报、凭据和未经审核的截图不进入公开运行时。
- 首个纵向切片收敛为一个公开项目、一个可执行问题和一组公开证据，以确定性回答建立最小闭环。
- 回答结构围绕背景、职责、方案、验证和状态组织，避免把计划、协作或学习记录夸大为独立交付成果。
- 当前状态：**已实现**，并成为后续所有能力扩展的事实与隐私底线。
- 相关设计：[实习作品集 V0 设计](superpowers/specs/2026-07-14-internship-portfolio-v0-design.md)

### 阶段二：提出第一版代码架构整理方向

- 针对早期后端中接口、应用、领域和基础设施边界混杂的问题，提出按职责重新组织代码的方案。
- 这一版方案建立了“公开作品集事实”和“回答能力”需要隔离的基本方向。
- 后续项目选择了更明确的模块化单体包结构，因此本阶段的具体重构计划不再作为当前实施依据。
- 当前状态：**已取代**。
- 相关设计：[早期代码架构重构设计](superpowers/specs/2026-07-14-codebase-architecture-refactor-design.md)

## 2026-07-15

### 阶段一：修正作品卡片的视觉重叠

- 在设计探索原型中处理项目卡片的遮挡和层级问题，明确内容可读性优先于装饰性重叠。
- 本阶段只影响历史设计探索，不代表当前生产前端的最终视觉规范。
- 当前状态：**已实现后被后续前端重建吸收**。
- 相关设计：[项目卡片重叠修正设计](superpowers/specs/2026-07-15-benchmark-project-card-overlap-design.md)

### 阶段二：形成动态公开内容 Agent 的总体设想

- 将早期静态作品集扩展方向拆为公开内容、时间线、Claim、Evidence、问答、治理、发布和模型表达等能力。
- 明确“公开运行数据”和“私有内容准备”必须分离，动态能力也不能绕过人工审核。
- 该总设计为后续 A/B/C 阶段提供方向，但其中未完成部分后来被更细的运行时可信、内容治理和未来智能设计接管。
- 当前状态：**已取代为分阶段权威设计**。
- 相关设计：[动态公开内容 Agent 设计](superpowers/specs/2026-07-15-dynamic-public-content-agent-design.md)

### 阶段三：收紧后端边界与隐私修复方向

- 针对早期后端评审暴露的模块泄漏、响应对象耦合和隐私扫描顺序问题，形成专项整改方案。
- 确定公共 API 只能返回公开 DTO，回答模块不得直接依赖作品集内部领域对象。
- 隐私检查被提升为发布前置条件，而不是制品生成后的补充动作。
- 当前状态：**已实现**，具体结构随后由模块化单体设计固化。
- 相关设计：[后端最终评审整改设计](superpowers/specs/2026-07-15-final-review-backend-remediation-design.md)

## 2026-07-16

### 阶段一：建立模块化单体后端

- 后端确立 `common / portfolio / answer` 三个顶层模块，在一个 Spring Boot 进程和一个交付制品内保持清晰边界。
- `portfolio` 负责公开事实、校验和查询；`answer` 负责问题理解与回答编排；跨模块读取通过专用 Gateway 完成。
- Answer 拥有自己的结果和证据表达，不再把 Portfolio 的领域对象直接泄漏到回答接口。
- 旧的通用分层包结构被移除，模块依赖方向成为项目长期架构基线。
- 当前状态：**已实现**。
- 相关设计：[模块化单体包设计](superpowers/specs/2026-07-16-modular-monolith-package-design.md)

### 阶段二：重建公开作品集前端

- 前端从早期设计原型演进为 Vue 3、TypeScript 和 Vite 的正式单页应用。
- 建立首页、项目目录、项目详情、时间线、证据中心和 Agent 工作台等主要页面骨架。
- 首页形成工程档案 Hero、可信度摘要、角色化提问和深入探索四层信息结构。
- Agent 工作台确立会话、对话和证据三栏结构，并坚持访客会话只存在当前页面内存。
- 当前状态：**已实现**；后续视觉、Case 信息架构和工作台交互继续演进。
- 相关设计：[作品集前端完整重建设计](superpowers/specs/2026-07-16-portfolio-frontend-full-rebuild-design.md)

### 阶段三：对齐前端原型的视觉与交互基线

- 正式前端开始吸收纸面卷宗原型的暖纸、暖黑、暗红和细线排版语言。
- 页面响应式、焦点、减少动态效果和工作台布局被纳入正式前端要求。
- 这一阶段建立的视觉方向有效，但具体工作台外壳、断点和交互细节后来由 7 月 22–24 日设计进一步取代。
- 当前状态：**部分被后续设计取代**。
- 相关设计：[前端原型保真对齐设计](superpowers/specs/2026-07-16-portfolio-prototype-fidelity-alignment-design.md)

### 阶段四：澄清查询结果与校验职责

- 查询服务返回自己的结果模型，公开响应 DTO 只在 Web 边界生成。
- 快照校验失败与单次查询失败被区分，避免局部错误污染整个公开内容读取流程。
- 当前状态：**已实现**。
- 相关计划：[结果与校验边界重构计划](superpowers/plans/2026-07-16-portfolio-result-validation-boundary-refactor.md)

## 2026-07-17

### 阶段一：公开内容从静态前端数据转向后端聚合 API

- 公共快照加入经过审核的 Timeline 事实，并要求时间线引用的项目和证据全部存在。
- 后端形成统一的公开内容聚合接口，使首页、项目、时间线、证据和问题预设共享同一版本的数据。
- 前端建立统一的加载、空状态、失败重试和未知资源处理，不再由不同页面各自维护数据副本。
- 当前状态：**已实现**。
- 相关设计：[公开内容 API 联调设计](superpowers/specs/2026-07-17-public-content-api-integration-design.md)

### 阶段二：作品集对话接入正式回答 API

- 首页轻量提问和完整 Agent 工作台开始使用后端回答接口，而不是只依赖前端固定演示文案。
- 对话请求增加过期结果保护，切换页面或问题后返回的旧结果不会覆盖当前状态。
- 前端与后端被整合为单一可打包制品，为后续运行时可信和发布门禁提供交付基线。
- 当前状态：**已实现**。
- 相关设计：[公开内容 API 联调设计](superpowers/specs/2026-07-17-public-content-api-integration-design.md)

## 2026-07-20

### 阶段一：确定运行时可信模型

- 回答契约从简单文本扩展为能够明确表达回答结果、生成方式、来源和核验状态的四维模型。
- 同一次回答只读取一个不可变公开快照，避免项目、证据和推荐问题来自不同内容版本。
- 访客问题和会话坚持不落库、不写日志、不进入 URL 或浏览器持久化存储。
- 未知问题、越界主体和能力不足场景采用明确的边界响应，不用猜测或无依据生成填补空白。
- 当前状态：**已实现**。
- 相关设计：[运行时可信设计](superpowers/specs/2026-07-20-portfolio-agent-runtime-trust-design.md)

## 2026-07-21

### 阶段一：建立 Claim、Evidence 与内容治理体系

- 公开内容从单一项目 JSON 演进为 Project、Claim、Evidence、Claim–Evidence 关联、TimelineEvent 和 QuestionPreset 等明确实体。
- Claim 只表达允许公开的事实陈述，Evidence 负责支持强度和公开状态，两者不再混为同一个对象。
- 候选准备、审核、人工批准、发布和回滚被拆成独立步骤，任何自动流程都不能替代内容所有者批准。
- 受控学习只允许产生待审核候选，不能直接改变公开运行时。
- 当前状态：**已实现**。
- 相关设计：[内容治理设计](superpowers/specs/2026-07-21-portfolio-agent-content-governance-design.md)

### 阶段二：把未来智能能力拆为 C1、C2 和 C3

- C1 定义为在固定公开事实基础上的可选模型表达，不允许模型改变事实范围。
- C2 定义为本地公开检索、固定只读工具和稳定引用式多轮，重点解决公开内容扩展后的可检索性和追问能力。
- C3 定义为更通用的 Provider、工具、Hook、编排和任务抽象，但必须逐项准入，不能因为形成总设计就视为全部可用。
- 当前状态：C1、C2 已进入后续实现；C3 只有 Model Provider Registry 获准并实现，其余能力仍未准入。
- 相关设计：[未来智能能力设计](superpowers/specs/2026-07-21-portfolio-agent-future-intelligence-design.md)

## 2026-07-22

### 阶段一：落地运行时可信与内容治理

- 四维回答契约、不可变快照、严格公开主体边界和页面内存会话进入正式运行时。
- 内容治理具备候选准备、审核、批准、发布和回滚所需的封闭流程。
- 发布包被区分为人工批准的规范公开载荷和服务器生成的运行制品，运行时只读取当前激活版本。
- 当前状态：**已实现**。
- 相关设计：[运行时可信设计](superpowers/specs/2026-07-20-portfolio-agent-runtime-trust-design.md)、[内容治理设计](superpowers/specs/2026-07-21-portfolio-agent-content-governance-design.md)

### 阶段二：实现 C1 受限模型表达

- 服务端先生成白名单 AnswerPlan，再允许单个选定 Provider 对公开事实进行语言表达。
- 模型草稿必须通过结构、长度、事实引用和公开范围检查；不合格结果回退到同一 AnswerPlan 的确定性表达。
- Provider 通过部署配置显式选择，不保存供应商会话标识，也不自动切换到其他 Provider。
- 当前状态：**受限启用、默认关闭**；需要模型开关、密钥和独立数据策略批准。
- 相关设计：[未来智能能力设计](superpowers/specs/2026-07-21-portfolio-agent-future-intelligence-design.md)

### 阶段三：实现 C2a 本地公开检索

- 技术选型确定为本地 BGE-small-zh-v1.5 INT8 ONNX，不向外部 Embedding 服务发送公开文档或访客查询。
- 检索路线由关键词、向量、RRF 融合和 Grounding Gate 组成；只有证据充分时才返回检索型回答。
- RAG 文档、关键词索引和向量索引与 ContentBundle 原子绑定，不能独立激活或回滚。
- 当前状态：**已实现、默认关闭**；运行时需要完整七文件 Bundle 和本地固定模型。
- 相关设计：[C2a 本地公开检索设计](superpowers/specs/2026-07-22-portfolio-agent-c2a-local-public-retrieval-design.md)

### 阶段四：实现 C2b 固定只读工具与引用式多轮

- 工具能力限定为查询项目、Claim、Evidence、时间线、公开搜索和项目比较六类固定读操作。
- 服务端根据封闭意图生成 ToolPlan，工具不能访问文件系统、网络、私有治理目录或写接口。
- 多轮上下文只保留公开内容版本和稳定实体引用，不保存历史问题、回答正文或 Provider thread。
- 内容版本变化或引用失效时重新核对并明确返回边界，而不是沿用过期事实。
- 当前状态：**已实现**。
- 相关设计：[未来智能能力设计](superpowers/specs/2026-07-21-portfolio-agent-future-intelligence-design.md)

### 阶段五：C3 只准入内建 Model Provider Registry

- Provider 选择从分散配置收敛到不可变 Registry 快照。
- Registry 只内建 DeepSeek V4 Flash 与 GLM-4.7，每次部署仍显式选择一个 Provider。
- 不提供自动故障转移、跨 Provider 重发或动态发现。
- Tool Registry、Hook、Orchestrator、多 Agent、DurableTask 和持久会话没有因此获得准入。
- 当前状态：Registry **已实现**；其余 C3 能力**未实现**。
- 相关设计：[C3 Model Provider Registry 设计](superpowers/specs/2026-07-22-portfolio-agent-c3-model-provider-registry-design.md)

### 阶段六：Agent 工作台转向浅色纸阶

- 三栏工作台从偏深色的实验界面调整为更清晰的浅色纸张层级。
- 深色实心控件被收敛到“新对话”和“发送”等关键动作，降低大面积深色带来的压迫感。
- 这套方案成为后续暖黑舞台和米色悬浮工作台的视觉前序。
- 当前状态：**已实现后被后续暖调作品窗口取代**。
- 相关设计：[浅色 Agent 工作台设计](superpowers/specs/2026-07-22-portfolio-agent-light-workspace-palette-design.md)

### 阶段七：确定求职作品集的内容补全路线

- 内容建设从“继续堆功能”调整为围绕长主线、具体功能、故障案例、评测和知识产出分层。
- 明确页面视觉与 Agent 能力需要按项目纵向切片共同交付，再做全站整合、质量收口和部署。
- 当前状态：**选定，持续实施中**。
- 相关设计：[求职作品集完成路线](superpowers/specs/2026-07-22-job-seeking-portfolio-completion-roadmap-design.md)

## 2026-07-23

### 阶段一：建立私有资产库与公开候选边界

- 对知识库中的项目、功能、故障、评测和学习资产建立统一登记，形成 68 项私有资产总账。
- 资产登记不等于公开发布；每项资产需要单独判断贡献类型、工作状态、隐私风险和公开价值。
- 候选内容与运行时公开 Bundle 分离，未经批准的素材继续保留在私有治理区。
- 当前状态：**已实现**。
- 相关设计：[作品集资产库摄取设计](superpowers/specs/2026-07-23-portfolio-asset-library-ingestion-design.md)

### 阶段二：Agent 工作台演进为暖黑舞台上的米色作品窗口

- Agent 页面由普通三栏布局演进为一个完整的米色工作台，悬浮在暖黑舞台之上。
- 桌面端保留三栏，较窄视口将证据栏和会话栏逐步降级为抽屉。
- Header、会话栏、对话区和证据区被统一为一个产品外壳，而不是多个互相竞争的大卡片。
- 当前状态：**已实现**。
- 相关设计：[生产响应式作品窗口设计](superpowers/specs/2026-07-23-agent-production-responsive-framed-workspace-design.md)

### 阶段三：重做本地会话与即时对话体验

- 左侧会话栏改为“新对话、会话标题列表、会话管理”的清晰结构。
- 推荐问题点击后立即形成用户消息并进入生成状态，不再要求二次确认。
- 新对话只有产生第一条用户消息后才进入历史，重命名后的标题不会被后续自动标题覆盖。
- 页面会话仍只存在当前标签页内存，刷新或关闭后消失。
- 当前状态：**已实现**。
- 相关设计：[响应式工作台设计](superpowers/specs/2026-07-23-agent-responsive-framed-workspace-design.md)

### 阶段四：建立证据、引用和来源三层工作台

- 右侧证据区从普通列表演进为“证据 / 引用 / 来源”三个明确视图。
- 当前回答引用的证据会优先聚焦，但不会隐藏项目中的其他公开证据。
- 回答段落可以定位对应引用，引用也可以返回并突出回答段落，形成双向追溯。
- 当前状态：**已实现**。
- 相关设计：[暖奶油配色与红色 CTA 设计](superpowers/specs/2026-07-23-agent-warm-cream-palette-and-red-cta-design.md)

### 阶段五：引入 CaseStudy 公共领域契约

- 作品集从只有 Project 主线扩展到可表达具体任务、故障和评测的 CaseStudy。
- 公共内容 Schema 演进到 3.0，并保留对旧 Schema 的兼容读取。
- 建立 CaseStudy 的领域模型、引用校验、查询服务和公开列表/详情 API。
- 第一批公开 Case 覆盖多语言图片保留、测试角色重置、CodeGraph 定性评测和 SQL 审计能力扩展。
- Case 只能使用自己的公开证据和预设，不能因为关联某个 Project 就自动扩展事实范围。
- 当前状态：**已实现**。
- 相关设计：[CaseStudy 公共契约设计](superpowers/specs/2026-07-23-portfolio-case-study-public-contract-design.md)

## 2026-07-24

### 阶段一：回答能力开始感知 Case

- 回答工具、项目案卷和 RAG 文档加入 Case 维度，使具体案例可以独立成为公开问答主体。
- Project 和 Case 的详情接口公开稳定实体标识，为跨页面引用和后续 Schema 演进提供基础。
- 项目案卷增加分节 Claim 追溯，页面可以说明某一段内容由哪些公开事实支持。
- 当前状态：**已实现**。
- 相关设计：[CaseStudy 公共契约设计](superpowers/specs/2026-07-23-portfolio-case-study-public-contract-design.md)

### 阶段二：建立真实本地检索对比体系

- 检索评估从少量人工观察演进为 Keyword、Vector 和 Hybrid 三条路线的统一基准。
- 评测关注召回位置、充分性判断和负向问题安全性，避免只用命中率掩盖错误回答风险。
- 报告与具体公开 Bundle、模型身份和策略版本绑定，后续内容变化需要重新评估。
- 当前状态：**已实现**；评测结论用于选择检索策略，不等于生产启用检索。
- 相关设计：[全量公开资产与混合检索评估设计](superpowers/specs/2026-07-24-full-public-assets-and-hybrid-retrieval-evaluation-design.md)、[检索基线评测报告](reports/retrieval-baseline-2026-07-24.md)

### 阶段三：扩展对话式 Agent v2

- 回答意图被拆为自然交流、通用知识、作品集事实、混合问题、时效问题和不支持/不安全问题。
- 对话窗口支持最多 20 轮临时上下文，但上下文不持久化，刷新后消失。
- 作品集回答通过细粒度公开上下文、固定只读工具和事实引用校验保持可追溯。
- 动态推荐问题必须先通过可回答性检查，不能推荐当前公开内容无法支持的问题。
- `/api/v2/answers` 成为独立对话式接口，并接入正式前端工作台。
- 当前状态：**受限启用**；自由模型对话需要访客数据策略审批，缺少条件时安全降级。
- 相关设计：[对话式能力扩展设计](superpowers/specs/2026-07-24-portfolio-agent-conversational-capability-expansion-design.md)

### 阶段四：修复模型结构化输出契约

- 模型输出从容易受供应商格式差异影响的松散文本，收敛为固定回答对象和分节结构。
- 动态推荐问题支持对象形式，不再因字段形态变化导致整份回答失败。
- 不完整、超长、结构非法或缺少公开引用的草稿不会直接展示。
- 当前状态：**已实现**。
- 相关设计：[Agent 结构化输出修复设计](superpowers/specs/2026-07-24-agent-structured-output-contract-fix-design.md)

### 阶段五：发布链路演进为可核对的七文件原子 Bundle

- 公开发布与资产决策账本绑定，Approval 对应精确的规范公开载荷。
- C2a 运行包由公开事实、展示配置、RAG 文档、关键词索引、向量索引、Manifest 和校验信息共同组成。
- 服务器只能导入完整且兼容的七文件版本；导入失败时保持旧 active 版本。
- 当前状态：**已实现、未部署**。
- 相关文档：[公开发布包契约](05-公开发布包契约.md)、[公开内容发布运行手册](06-公开内容发布运行手册.md)

## 2026-07-27

### 阶段一：完成第一波公开资产治理

- 第一波候选从资产总账中按明确公开边界选出，并与留出问题集隔离。
- 治理流程封堵了绕过 Approval、复用过期批准和改变规范载荷后继续发布等路径。
- 公开内容的人工决定被作为事实保存，后续构建不能擅自改写资产结论。
- 当前状态：**已实现**。
- 相关文档：[作品集资产库状态](09-作品集资产库状态.md)

### 阶段二：检索策略演进到 v2.1

- 第一版策略在高风险负向问题上存在“看似相关但不足以回答”的误判，因而被拒绝。
- 新策略提高充分性判断要求，并保留对安全说明类问题的正常回答能力。
- 第一波公开资产被纳入本地运行包，检索策略和内容版本同步演进。
- 当前状态：策略 v2.1 **已实现**；运行时 Hybrid 仍**默认关闭**。
- 相关报告：[第一波检索策略评测报告](reports/retrieval-wave-1-policy-v2-1-2026-07-27.md)

### 阶段三：前端完成设计语言与可访问性收口

- 全站颜色、间距、圆角和字体令牌被统一，自托管中文衬线字体替代不稳定的外部字体依赖。
- Agent 补齐键盘焦点、触屏操作、证据卡交互语义、当前导航状态和回答标签共用模型。
- 对话输入区、容器滚动、时间线和页面层级进一步统一，历史工作台数值由当前实现令牌取代。
- 当前状态：**已实现**。
- 相关设计：[前端设计整改设计](superpowers/specs/2026-07-24-frontend-design-remediation-design.md)

## 2026-07-28

### 阶段一：扩充全量公开资产并完成第二波检索比较

- 公开内容从第一波少量资产扩展到 61 项可公开资产，7 项高风险或不适合公开的内容继续保持 `EXCLUDE`。
- 项目主线、具体 Case、Claim、Evidence、Timeline 和问题预设随内容扩充同步增长。
- 全量内容继续沿用 Keyword、Vector 和 Hybrid 三路比较，用安全充分性约束判断混合检索的实际价值。
- 当前状态：内容与评测**已实现**；生产部署**未完成**。
- 相关设计：[全量公开资产与混合检索评估设计](superpowers/specs/2026-07-24-full-public-assets-and-hybrid-retrieval-evaluation-design.md)、[全量公开资产检索评测报告](reports/retrieval-full-public-assets-candidate-2026-07-27.md)

### 阶段二：关闭 Case 后端与发布链路缺口

- Answer 请求正式支持 `source=CASE`，并要求 Project 与 Case 上下文互斥。
- 未知 Project 或 Case 在路由、工具和 Provider 之前直接关闭，避免模型接触未经确认的主体。
- 随包运行时加入 Case API 和 Case 问答能力，发布流程增加显式真实 Provider 验收入口。
- 普通自动化流程继续保持确定性，不能被误当作真实 Provider 调用证据。
- 当前状态：后端与发布闭环**已实现**；真实 Provider 外部验收和生产部署仍**未完成**。
- 相关设计：[V1 Case 与发布收尾设计](superpowers/specs/2026-07-28-portfolio-v1-case-and-release-closure-design.md)

### 阶段三：确定 Agent 生产可用性边界

- 对话请求增加客户端生成的幂等标识，并显式返回生成状态、降级状态和通知代码。
- 生产保护范围扩展到匿名来源预算、请求速率、并发数量、Provider 时间预算和后端总处理预算。
- 项目选择使用进程内短期状态实现保护，不引入数据库、账号体系或持久会话。
- 当前状态：设计**选定**，基础准入控制于当日开始实现。
- 相关设计：[Agent 生产可用性设计](superpowers/specs/2026-07-28-agent-production-readiness-design.md)

## 2026-07-29

### 阶段一：完成 Agent 生产请求保护

- 相同匿名来源和请求标识在短时间内复用同一执行结果，避免重试造成重复 Provider 调用。
- 服务端统一处理 Provider 超时、总处理超时、每分钟请求数和最大并发数。
- 前端支持主动取消、超时提示和复用原请求标识的安全重试。
- 代理来源默认不受信任，只有明确配置可信代理时才读取转发地址。
- 缓存清理由无界扫描调整为有预算的增量处理，避免保护机制本身形成性能风险。
- 当前状态：**已实现**。
- 相关设计：[Agent 生产可用性设计](superpowers/specs/2026-07-28-agent-production-readiness-design.md)

### 阶段二：独立 Case 信息架构进入正式前端

- 前端新增独立 Case 目录和 Case 详情页，案例不再借用 Project 路径表达。
- Case 详情展示问题、动作、判断、验证、结果、边界、证据和建议问题。
- Case 到 Agent 通过随机、短时、一次性页面内存交接传递上下文，问题不会进入 URL、历史记录或持久化存储。
- 首页增加 Case 入口，旧案例路径开始转向规范 Case 地址。
- 当前状态：**已实现**。
- 相关设计：[V1 Case 与发布收尾设计](superpowers/specs/2026-07-28-portfolio-v1-case-and-release-closure-design.md)

### 阶段三：建立端到端可观测性与统一错误契约

- 后端采用结构化诊断事件和请求关联标识，区分请求开始、完成、失败和内部能力阶段。
- 前端将请求关联标识带入错误恢复体验，用户可以在不暴露问题内容的前提下提供排障线索。
- 日志契约明确禁止访客问题、回答正文、原始 IP、Provider 载荷、密钥、请求体和原始堆栈进入诊断事件。
- 浏览器诊断入口默认关闭，只接受封闭、限流且不持久化的事件类型。
- 当前状态：**已实现**。
- 相关设计：[可观测日志系统设计](superpowers/specs/2026-07-29-observability-logging-system-design.md)

### 阶段四：统一导航并重建 Project、Case、Collection 领域关系

- 全站顶栏收敛为一套路由导航，首页不再维护独立导航特例。
- Project 被明确为长期工程主线，Case 被明确为一次具体任务、问题、事故或评测记录。
- Collection 被定义为浏览策展集合，不再承担成果归属或 Agent 问答主体职责。
- 一个 Case 最多归属一个 Project，但可以出现在多个 Collection；没有 Project 的 Case 仍是合法独立案例。
- 公开内容开始迁移到 Schema 4.0，为新的项目主线和案例浏览方式提供数据基础。
- 当前状态：**已实现**。
- 相关设计：[项目与案例信息架构设计](superpowers/specs/2026-07-29-project-case-information-architecture-design.md)

## 2026-07-30

### 阶段一：项目与案例页面采用新的双层信息架构

- 项目目录从混合卡片集合调整为主要项目和次级项目两层主线展示。
- 项目详情加入相关 Case 区，使长期项目叙事可以下钻到具体任务与问题。
- Case 目录支持按状态、Project 和 Collection 组合筛选，独立 Case 不再被迫归入虚构项目。
- 旧集合型 Project 路径转向 Case 筛选视图，避免用 Project 名义表达纯策展集合。
- 当前状态：**已实现**。
- 相关设计：[项目与案例信息架构设计](superpowers/specs/2026-07-29-project-case-information-architecture-design.md)、[前端设计交接](handoffs/2026-07-29-project-case-frontend-design-handoff.md)

### 阶段二：选择 PostgreSQL 双库而不是直接替换现有 Bundle

- 数据库能力被拆为物理隔离的公开运行库和私有治理库。
- 公开运行库保存规范化公开投影、不可变兼容快照、active release、全文/向量检索文档和组合推荐标签。
- 私有治理库只保存操作者显式选择的 Markdown 文档、修订、分块、向量状态和链接建议，公开 API 不读取该库。
- 默认启动继续使用随包 JSON Bundle；只有显式启用公开数据库时才切换仓储。
- 当前状态：**已实现、默认关闭、未部署**。
- 相关设计：[PostgreSQL 作品集组合设计](superpowers/specs/2026-07-30-postgresql-portfolio-composition-design.md)、[PostgreSQL 演进决策](decisions/2026-07-30-postgresql-portfolio-composition-evolution.md)

### 阶段三：实现 Markdown 增量治理导入

- 私有治理从一次性候选文件演进为操作者显式触发的 Markdown 扫描和增量导入。
- 文档修订、内容分块、向量状态和链接建议可以独立更新，不需要把整个知识库重新发布。
- 项目明确不增加自动文件监听；未审核 Markdown 不会自动进入公开运行时。
- 当前状态：**已实现、默认关闭**。
- 相关设计：[PostgreSQL 作品集组合设计](superpowers/specs/2026-07-30-postgresql-portfolio-composition-design.md)

### 阶段四：实现混合召回与互补作品集组合

- 新能力不是简单返回单个最相似项目，而是从 Project 和 Case 中召回候选后，确定性组合 2–5 项互补材料。
- 组合同时考虑相关性、能力覆盖、重复度、公开证据和当前 release，检索结果不能绕过人工批准。
- 选型保留确定性约束作为最终决策层，避免让模型自由决定哪些经历进入公开组合。
- 该阶段曾把能力作为仅在公开 PostgreSQL 数据源启用时注册的独立接口提供；该产品表面已在 2026-07-31 收口，内部算法继续复用。
- 当前状态：**内部能力已实现、默认关闭、未部署；独立公开接口已移除**。
- 相关设计：[PostgreSQL 作品集组合设计](superpowers/specs/2026-07-30-postgresql-portfolio-composition-design.md)、[组合能力前端交接](handoffs/2026-07-30-portfolio-composition-frontend-handoff.md)

### 阶段五：建立 R0–R4 组合策略评测与迁移门禁

- 组合能力建立从基础相关性到互补性、隐私边界和迁移一致性的分层评测口径。
- 文件 Bundle 与数据库投影需要通过同口径比较，数据库不能因为结构变化而改变公开事实或扩大可见范围。
- 发布制品隐私扫描从固定目录检查扩展为递归扫描，覆盖数据库迁移、导入制品和新增发布材料。
- 当前状态：**已实现**；真实数据库环境与生产发布仍**未完成**。
- 相关设计：[PostgreSQL 作品集组合设计](superpowers/specs/2026-07-30-postgresql-portfolio-composition-design.md)

### 阶段六：补齐本地 PostgreSQL / pgvector 运行入口

- 本地开发环境采用一个 PostgreSQL 16 / pgvector 容器承载两个物理独立数据库和两个独立 owner 账号。
- 公开数据库用于 Release、Project、Case、Claim、Evidence、检索文档和公开向量；治理数据库用于私有 Markdown、修订、分块、导入记录和私有向量。
- 数据库端口只绑定本机回环地址，不附带额外管理后台；环境变量模板与本地密钥文件分离。
- 公网环境只允许部署公开数据库，治理数据库、原始 Markdown、私有向量和本地模型不得进入公网服务器。
- 当前状态：**已实现、仅限本地开发、未部署**。
- 相关文档：[本地 PostgreSQL / pgvector 运行手册](10-本地PostgreSQL与pgvector运行手册.md)

## 2026-07-31

### 阶段一：Agent 运行时接入作品集硬路由与无状态推荐

- `/api/v2/answers` 已在通用模型生成前接入 `PortfolioTaskResolver` 与 `PortfolioIntelligence`：确定性作品集规则、Project/Case 上下文、推荐上下文，以及外层路由判定为作品集或混合意图时，统一转入新检索 seam。
- Project/Case 上下文只在边界层使用 slug；通过公开内容校验后会转换为稳定作品 ID。hint 不再默认推断 `FACT_LOOKUP`；每个作品集硬路由最多调用一次 `portfolio_task` 分类，由一个互斥决策返回 unsafe/time 边界或任务。外层 `PORTFOLIO_GROUNDED` 与 `HYBRID` 路径同样直接消费该决策，boundary 安全返回，task 原样进入 intelligence，避免二次 resolve。精确主体约束只用于最终确认为单主体事实查询的任务，推荐、推荐调整和比较仍面向全局公开候选，Bundle 与 PostgreSQL 检索遵守同一规则。
- 安全与时效边界复用确定性门禁并扩展到同一次任务分类：确定性判断最先执行，未知主体校验先于任何模型调用；合法模型边界可抢占，确定性任务规则优先于模型普通任务，未命中规则才采用高置信模型任务。Provider 禁用时保持确定性 intelligence，未命中的 hint-only 请求澄清且零模型调用；诊断不记录原问题，全程不维护第二套敏感词或时效词典。
- 混合意图先取得统一 intelligence 材料并显式映射为 grounding，再进入模型生成与验证；成功响应保留 `HYBRID` 意图/范围，澄清、无证据、模型或验证失败时保留同一批安全的确定性作品材料，并把回答范围降为 `PORTFOLIO`。
- 一般对话、时效问题和安全边界继续沿用原链路；事实与比较回答直接消费统一 retriever 的公开 Claim/Evidence 材料，不再回落到旧作品集 grounding 召回链。
- 推荐结果由确定性策略直接写入 `portfolioRecommendation`，完整上下文可无状态回传并重新校验；模型禁用、不可用或引导重建均不会改变推荐集合与顺序，合法空推荐仍保留结构化对象。
- Spring 在数据库关闭与启用两种模式下均只装配一个 resolver、validator、policy、context validator、intelligence、assembler 和 retriever；数据库主检索不可用时仍由既有 failover seam 回退随包 Bundle。
- 新增安全诊断只记录任务模式、主体/证据/推荐数量、上下文是否存在、验证结果和耗时档位，不记录问题、批次 ID、上下文正文或作品正文。
- 当前状态：**已实现并接入公开 Agent v2 运行时；生产 Provider 与数据库部署仍需独立验收**。
- 相关设计：[Agent作品集智能检索与确定性推荐设计](superpowers/specs/2026-07-31-portfolio-intelligence-hard-routing-design.md)

### 阶段二：建立无状态作品集任务硬路由

- 作品集事实查询、比较、首次推荐和推荐调整收敛到内部 `PortfolioIntelligence` 模块；每种任务模式都经由固定的校验与处理分支，不向模型开放检索、候选或排序决策。
- 推荐继续使用统一检索结果和确定性策略。检索投影显式携带职业方向、真实 Evidence 公开状态和确定性评分元数据，并仅把已召回的 Claim 与 Evidence 映射为候选证据，避免凭空构造引用、状态或评分依据。
- 推荐调整会按回传的有序作品 ID 在当前公开 Release 中独立重取并校验内容版本、批次指纹、公开作品和证据约束，不用续问文本缩小旧结果校验范围；合法空推荐集仍校验 Release、指纹与受控条件，但不发起数据库 ID 数组查询；重复、空白、超出请求数量或超过 5 个的 ID 会在检索前转为结构化追问。校验通过后才合并受控调整条件并重新计算，后端不保存推荐上下文、问题或批次状态。
- 缺失受众角色、调整上下文或调整指令，以及上下文失配时，模块只返回一个结构化追问，不启动隐式推荐。
- 当前状态：**已实现内部硬路由，并已由 Agent v2 运行时和 HTTP 契约接入**。
- 相关设计：[Agent作品集智能检索与确定性推荐设计](superpowers/specs/2026-07-31-portfolio-intelligence-hard-routing-design.md)

### 阶段三：为作品集推荐建立无状态回答响应契约

- Agent 回答结果现在可以携带可选的结构化作品集推荐；既有构造方式和引导式重建仍保持兼容，并会保留推荐结果。
- 推荐响应以公开作品 ID、公开证据 ID、受控条件、内容版本和确定性批次指纹表达上下文，不携带访客原问题或自由文本目标。
- 普通回答不会输出推荐字段；推荐任务即使暂未选出条目，也会完整返回推荐对象和上下文，便于客户端安全地发起后续调整。
- 当前状态：**已实现回答契约，并已由运行时硬路由填充**。
- 相关设计：[Agent作品集智能检索与确定性推荐设计](superpowers/specs/2026-07-31-portfolio-intelligence-hard-routing-design.md)

### 阶段四：建立统一检索适配层并预接 PostgreSQL 主检索

- 作品集事实、比较和推荐路径现在共享显式的检索领域结果形状；结果只承载公开主体、已验证 Claim 对应的通过 Evidence 的材料，以及检索来源。
- Bundle 检索适配器直接复用现有本地关键词、向量、RRF 与 Grounding Gate 协调器，并只把发布语料中被选中的 chunk/Claim 映射为事实段落；PostgreSQL/pgvector 适配器复用既有参数化召回，同时用固定只读查询返回 VERIFIED Claim 的真实公开陈述。
- Bundle 主体标识从路由 slug 中解耦为不可变作品 ID；项目职业方向取自身公开投影，Case 职业方向为空，双方能力均只来自各自主体的 VERIFIED Claim topics，不从 technologies 或父项目继承；PostgreSQL 精确上下文复核遵守同一主体语义，不让 Case 继承父项目职业方向。职业方向作为确定性资格条件，排序后的职业/能力条件只补充统一协调器的检索文本，受众角色不参与算法切换。
- 公开数据库导入同样只投影 VERIFIED Claim topics，并统一执行 trim 与大写规范化；V3 幂等迁移优先从 runtime snapshot 的原始 claims/topics 重建能力投影，无 snapshot 的遗留 release 则从仍有 VERIFIED 支持的旧行归并重建，以恢复被旧主键掩盖的合法能力并消除大小写重复。
- Spring 装配现在在公开数据库启用时提供唯一的 failover 检索 seam：PostgreSQL/pgvector 主检索只有因基础设施不可用而抛出 `PortfolioRetrievalException` 时才切换到独立加载的随包 Bundle，并以固定降级标记返回；该 fallback 不复用数据库支持的知识 gateway，因而不会在冷启动主库故障时再次访问同一数据库。参数、状态和编程错误保持原样失败。数据库关闭或未配置时则只注入 Bundle seam。
- Agent runtime 与公开 HTTP 表面已接入该 seam；一般对话、时效与安全边界仍保持既有行为。
- 当前状态：**已实现内部适配层、受控 Spring 装配与运行时集成**。
- 相关设计：[Agent作品集智能检索与确定性推荐设计](superpowers/specs/2026-07-31-portfolio-intelligence-hard-routing-design.md)

### 阶段五：收口公开 Selection HTTP 表面

- 作品集查询、比较、推荐和推荐调整统一通过 `POST /api/v2/answers` 进入 Agent；旧独立 Selection Controller、专用请求/响应 DTO 和 HTTP Mapper 不再作为生产源码存在，也不保留兼容期。
- PostgreSQL/pgvector 在公开数据库启用时继续作为 Agent 内部主检索，基础设施不可用时受控降级到随包 Bundle；候选召回、确定性选择策略、领域服务和离线 benchmark 继续保留为内部能力。
- 对话与推荐上下文只由当前标签页内存持有并随请求回传，刷新或关闭标签页即清空；后端不保存推荐上下文、访客问题或会话状态。
- 当前状态：**已实现；独立 Selection POST 返回 404，内部 benchmark 可继续运行**。
- 相关设计：[Agent作品集智能检索与确定性推荐设计](superpowers/specs/2026-07-31-portfolio-intelligence-hard-routing-design.md)

### 阶段六：以显式安全启动替代根目录 `.env` 假设

- 本地模型启动改为从仓库外 Secret 文件显式加载配置，不再假设 Spring Boot、Maven 或 JAR 会自动读取根目录 `.env`。
- 启动前统一核对四个批准开关、内建 Provider、对应密钥、Java 21、Maven、Node、前端依赖和端口。
- 前后端由同一入口托管，只清理本次创建的进程；已占用端口不会被脚本接管或终止。
- 配置完整不再等同于 AI 可用。脚本使用固定公开问题探测 Provider，只有经过既有回答校验的真实 `MODEL` 结果才报告连接成功，其余情况明确保留降级状态。
- 当前状态：**已实现、仅限本地开发；真实 Provider 仍需使用仓库外有效 Secret 单独验收**。
- 相关设计：[Agent 动态引导、会话标题与安全本地启动设计](superpowers/specs/2026-07-30-agent-guidance-and-local-startup-design.md)

### 阶段七：收紧发布前代码与隐私门禁

- Java 生产代码和测试统一使用显式类型与普通不可变类，发布检查不再接受 `var` 或 `record`。
- 仓库隐私扫描能够区分明文凭据与环境变量间接引用，并允许受控的本地 PostgreSQL 配置文件名；根目录不再保留 Secret 文件。
- 治理测试账本跟随 Schema 4 公开 Bundle 补齐学习状态、独立 Evidence 和跨版本基准映射，失败信息可直接定位资产与公开内容。
- JAR 浏览器验收使用独立的测试请求配额，避免双浏览器项目共用回环地址时触发生产限流；生产默认限流保持不变。
- 当前状态：**已实现**。

### 阶段八：完善 Agent 对话页的会话标题、对话进度与引导问题

- 会话标题在数据层保留首个问题的完整规范化文本，主标题两行、侧栏单行只做视觉省略，悬停与屏幕阅读器可读完整标题；手动重命名永久锁定自动标题。
- 每个会话独立维护对话进度 coveredTopics 并随下一轮请求回传，重试使用失败当时的进度快照，多会话互不污染；对话进度只保存在页面内存中。
- 每条回答固定展示自己的三个引导问题，点击后携带其 Project/Case 目标上下文；后端契约异常时从公开问题预设确定性补足，恢复诊断只记录错误类别、数量和阶段。
- AI 降级与网络传输失败明确区分：降级回答正常展示并可连续对话，网络失败保留重试并提供本地公开问题。
- 当前状态：**已实现**。
- 相关设计：[Agent 动态引导、会话标题与安全本地启动设计](superpowers/specs/2026-07-30-agent-guidance-and-local-startup-design.md)、[引导能力前端交接](handoffs/2026-07-31-agent-guidance-frontend-handoff.md)

### 阶段九：完成对话内结构化作品推荐的前后端契约

- 在 Agent 对话消息内新增可选的 `portfolioRecommendation` 卡片组：序号、标题、匹配理由、菱形点证据入口与「查看作品」链接，视觉语汇复刻现有证据卡/资产卡底盘，不引入新颜色。
- 推荐项严格按后端返回顺序渲染，前端不重排、不增删、不筛选、不造排行榜或「最佳/命中」夸张文案；`satisfiedConstraints` 低强调展示，仅 `unsatisfiedConstraints` 存在时才提示，空推荐不伪造作品卡。
- 映射层对推荐结构做运行时校验：非法结构不破坏可信文本回答，仅忽略推荐并经现有安全诊断入口上报脱敏事件（不含问题、批次 ID 或作品内容）。
- 推荐调整操作（换掉这个 / 为什么推荐 / 再偏后端一点 / 把数量改成 N 个）会在请求中回传完整的受控推荐上下文；普通问题不携带陈旧批次，新建会话后不恢复推荐上下文。上下文仅存当前标签页内存，后端每轮都基于当前公开 Release 重新校验。
- 评定方向：先经三版高保真原型（保守 / 档案索引 / 规格表）评审，选定保守版作为落地视觉基准。
- 后端 `/api/v2/answers` 已返回结构化推荐与完整无状态上下文；前端仅按后端顺序渲染并回传，不重排、不增删、不把推荐上下文写入浏览器存储。模型不能决定推荐集合，非法结构只会被前端忽略并以脱敏诊断记录。
- 当前状态：**已实现并完成前后端契约接通；真实 Provider、PostgreSQL 环境与生产部署仍需独立验收**。
- 相关设计：[Agent作品集智能检索与确定性推荐设计](superpowers/specs/2026-07-31-portfolio-intelligence-hard-routing-design.md)、作品推荐前端契约（见 `design/demos/作品推荐-V1保守-对话内嵌.html` 视觉基准）

## 2026-08-03

### 阶段一：补齐公开项目详细介绍的意图识别

- 对“详细介绍某个公开项目”的提问补充了明确的作品集事实查询识别，使其进入既有公开主体校验、统一检索和证据约束链路，而不是被误判为泛化对话。
- 修复只扩大已批准公开 Project 的可达问法，不改变主体边界、推荐规则、模型数据边界或公开内容范围。
- 当前状态：**已实现**。

### 阶段二：完成 PortfolioIntelligence 单一内核收敛

- 删除 `POST /api/v1/answers` 及旧 AnswerService / QuestionResolver 独立决策链；旧路径现在返回 404，`POST /api/v2/answers` 成为唯一公开回答入口，不设置迁移期。
- 作品集预设、主体规则、模型分类、检索规划、证据策略、推荐和结果语义统一由 `PortfolioIntelligence.tryResolve(PortfolioTurn)` 决定。外层只保留全局安全、时间、问候和通用对话，只有 PI 返回 `NOT_PORTFOLIO` 才转交通用模型。
- PI 内部优先级固定为显式引用、正式预设、高精度规则、受约束模型分类。页面 Project/Case hint 只提供主体范围，不会把无关通用问题强制改写为作品集问题；“这个项目”等明确指代仍可结合当前页面主体解析。
- v2 结果改为分别表达 `resolution`、`answerScope`、`constructionMode`、`intentSource`、`evidenceState`、`degraded` 和 `noticeCode`；空证据固定为 `NOT_SUPPORTED + INSUFFICIENT`，不再用 `BOUNDARY` 或 `generationMode` 混合业务与技术语义。
- 删除旧 `PortfolioAgentRuntime`、C2b ToolPlan/ContextEnvelope 编排和 Model Expression 决策岛。引用式多轮改为显式 `PortfolioReferenceContext` + `PortfolioRetrievalPlanner`：只有结构化按钮发送引用，自由输入不继承，刷新或新建会话清空；版本更新但引用有效时返回 `contextVersionUpdated=true`，失效时返回 `NEEDS_CLARIFICATION`。
- PostgreSQL/pgvector 与 Bundle 共享主体和相关性语义；主检索基础设施故障时按类型切换 Bundle，两源均失败时返回 `PORTFOLIO_RETRIEVAL_FAILED`（503），不会伪装成业务证据不足。
- local Profile 的日志路径统一到仓库根 `logs/current/`。Logback 独占后端 info/error 文件，启动器独占前端 info/error 与 launcher 文件；IntelliJ、Maven 和正式本地启动脚本使用同一仓库标记解析，也支持 `PORTFOLIO_LOG_DIRECTORY` 显式覆盖。
- 前端三栏布局、暖调视觉和主要交互骨架保持不变；变化只集中在状态标签、显式引用按钮的请求载荷和刷新后的引用清理。
- 验证结果：后端 766 个测试通过（5 个跳过），前端 41 个测试文件共 442 个测试通过，前端生产构建通过，本地日志路由、启动器与真实 Provider 响应断言脚本测试通过；实际 Spring Boot local 启动已创建 `backend-info.log` 与 `backend-error.log`。
- 当前状态：**已实现并完成本地自动化验证；真实 Provider、PostgreSQL 环境与生产部署仍需独立验收**。
- 相关设计：[PortfolioIntelligence 单一内核收敛设计](superpowers/specs/2026-08-03-portfolio-intelligence-convergence-design.md)


## 2026-08-04

### 阶段一：统一前端开发服务器端口与直接启动日志

- 前端本地默认端口从 `5174` 统一为 `5173`，`scripts/start-local.ps1` 与直接 `npm run dev` 都强制 `--strictPort`：端口被占用时明确失败退出，而不是静默迁移到其他地址。
- 直接 `npm run dev` 改由仓库自有入口 `scripts/start-frontend.ps1` 接管：创建与启动器同一个 `LocalLogRouter`，把 Vite stdout/stderr 脱敏后镜像到终端并写入 `frontend-info.log`、`frontend-error.log`，Vite 退出码原样传播；日志布局无法解析或路由器初始化失败时降级为 console-only，不阻断 Vite。
- 在 `start-local.ps1` 托管下，前端入口只镜像不落盘（`PORTFOLIO_FRONTEND_LOG_OWNER=UNIFIED` 委托），保持启动器单写者所有权，避免同一文件双写。
- 浏览器诊断开关修正：启动器改用 `PORTFOLIO_DIAGNOSTICS_FRONTEND_INGEST_ENABLED=true` 直接绑定 `portfolio.diagnostics.frontend-ingest-enabled`；旧变量仅保留为生产配置别名。直接 `npm run dev` 不自动开启该端点。
- `local` Profile 的 console 输出保留诊断键值对（含 `event.origin=browser`）供本地路由器分类；后端文件 appender 通过 `BrowserOriginEventFilter` 排除浏览器事件，保证每条事件只有一个语义目的地。
- 路由器脱敏规则补齐 `file:///` 正斜杠形式的仓库/用户主目录路径替换，防止 Vite 堆栈行泄漏本地路径。
- 当前状态：**已实现并完成本地自动化验证与真实 Vite 端到端验证（默认端口服务、日志落盘、镜像输出、端口占用失败、路径脱敏）；真实 `start-local.ps1` 全链路（含模型密钥）与生产部署仍需独立验收**。
- 相关设计：[Direct Vite Local Logging Design](superpowers/specs/2026-08-04-direct-vite-local-logging-design.md)

### 阶段二：将周末登录奖励 ABTest 建成独立作品集 Project

- 将一次重要 ABTest 活动从“活动系统协作项”提升为独立 `PRIMARY / DELIVERED` Project，并用三个关联 Case 分别呈现实验设计与稳定分流、服务端条件能力与配置演进、验证观测与风险控制。
- 公开叙事覆盖需求翻译、实验单位、登录天数分层、层内稳定分桶、标签持久化、分配与曝光区分、配置/缓存/执行链路、测试矩阵、停止条件和回滚预案；不公开原始代码、SQL、内部标识和不可验证指标。
- schema 4.0 Bundle 升级到 `2026-08-04.2`，规模为 6 Project、52 Case、83 Claim、63 APPROVED Evidence、83 links/chunks；候选已通过人工 Approval、治理基准、隐私与发布完整性校验。
- 新增内容改变全局检索统计后，正式预设曾受相关性阈值影响；现已将正式预设固定为受控主体精确检索，自由文本路径仍保持原相关性与 Grounding Gate。
- 发布门禁同时关闭了 v2 `referenceContext` mock、生产 ECS stdout、MDC 重复字段、active 指针 BOM 和 PostgreSQL 双库主 Bean 五项回归；治理 CLI 94 个场景及打包 JAR 的 62 个桌面/移动场景均完成验证，其中 18 个依运行条件按设计跳过。
- 本地 PostgreSQL 已原子导入并激活 `2026-08-04.2`：6 Project、52 Case、83 Claim、63 Evidence、83 个检索文档与 embedding；治理库保持 2 documents / 2 revisions / 4 chunks 不变。
- 当前状态：**已完成本地内容发布、随包运行时、打包应用与 PostgreSQL 验收；未生产部署，不声明线上实验收益或真实 Provider 验收**。
- 相关设计：[周末登录奖励 ABTest 作品集项目设计](superpowers/specs/2026-08-04-weekend-login-abtest-portfolio-project-design.md)

## 2026-08-05

### Preset Contract 端到端闭环纠偏

- 根因：内容分支 86e2c9 基于旧基线重生成 bundle 时丢失 Contract 字段，合并提交接受了该生成物，QuestionDefinition 把缺失字段兼容为“空 Claim 列表 + DRAFT”，导致运行 bundle 的 Active Contract 从 15 退化到 0；同时结构化主体 suggestion 的生效仍受问句指代词词表控制，projectSlug 被忽略后落入 GENERAL + CAPABILITY_UNAVAILABLE。
- 修复一（治理投影）：新增 preset-contract-policy.v1.json 精确声明 18 个单主体 Active Contract 与 1 个跨主体 Draft 清单；prepare-candidate 在事实 patch 合并后、写盘前执行 Invoke-PresetContractProjection，从 policy 投影 contractSubjectId、Required/Supporting Claims、Evidence Requirement 与状态，并计算单项 pcv1-* 版本与 Active 集合 sha256:* 指纹；alidate / benchmark / build-review-pack / approve / publish / verify 均重算并比对指纹，旧基线重生成、缺字段、Active 集合漂移与 hash 漂移均以七个类型化失败码阻断。
- 修复二（运行时门禁）：QuestionDefinition 新增 contractSubjectId 作为执行主体唯一来源，版本算法不再从展示关联推断主体且不再排序 aliases；PortfolioSnapshotValidator 与 PublicBundleLoader 对 Active Contract 校验主体存在性、展示关联、同主体 Claim、最低公开证据与 manifest presetContractSetHash，检索型 bundle 缺字段直接加载失败；运行时按 contractSubjectId 装配 preset，DRAFT 不再进入 runtime questions。
- 修复三（路由与展示）：新增 StructuredSubjectTaskResolver，在模型分类前按 slug 解析主体，未知/重复 slug 返回 INVALID_INPUT + STRUCTURED_SUBJECT_INVALID，已知 slug 进入 SUBJECT_SCOPED_RELEVANCE，不再受问句指代词控制；公共 DTO 只输出 ACTIVE preset（含版本），前端加载时过滤无版本/格式非法的 preset 并以脱敏诊断记录，正式 preset 点击始终携带 ID/版本与主体 slug，普通 suggestion 不携带 Contract 身份。
- 兼容资源 public-portfolio.v1.json 不再手工维护 Contract 状态，已与当前 bundle 对齐为无 Contract 的 Draft 状态，待新 bundle 发布后从同一 policy 投影重新生成。
- 验证结果：后端 846 个测试全部通过，前端 42 个测试文件共 455 个测试通过且生产构建通过；治理 CLI 101 个场景、policy/closure 脚本与隐私检查全部通过。
- 发布记录：2026-08-05.1 候选经治理链人工 Approval（APR-5bfdbcd6、APR-fa8c3c6，均绑定 presetContractSetHash=sha256:7183f29f…）后发布并完成 RELEASE_VERIFIED 审计，active 指针已切换；随包七文件 bundle 已整体替换为发布产物，PublicBundleVerificationCli 输出 18 个 Active Contract 且 set hash 与 Java 重算一致。兼容资源 public-portfolio.v1.json 已从发布候选投影重新生成，携带与 bundle 相同的 sql-audit-overview 契约主体、Claims、Evidence 与版本。
- 当前状态：**端到端闭环完成——发布、随包替换、真实 bundle 截图请求回放（SQL 正式问题 ANSWERED/PRESET、角色重置 suggestion ANSWERED/RULE、未知 slug INVALID_INPUT）与全量自动化验证均通过；本地 PostgreSQL 一致性验收与生产部署仍待独立执行**。
- 相关设计：[Preset Contract 端到端闭环纠偏设计](superpowers/specs/2026-08-05-preset-contract-end-to-end-closure-design.md)
