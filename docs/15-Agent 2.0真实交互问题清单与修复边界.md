# Agent 2.0 开放缺陷与开发账本
<!-- DOCUMENT_STATUS: CURRENT_AUTHORITY -->

> **文档性质：** Agent 2.0 开放生产问题与已批准架构、验证、文档治理工作的唯一账本。
> **总体状态：** `IN_PROGRESS`。
> **本轮边界：** 仅重构治理文档；不改变生产代码、Frontend、Prompt、公开合同、OperationBinding 或 Provider 配置；不调用真实 Provider，Provider Gate 为 `NOT_APPLICABLE`。

## 1. 当前结论与证据边界

当前 Command → Goal → Plan → Execution → Projection → State 单生产链仍成立，幂等、取消、原子结算、公开证据和 typed discussion pointer 继续作为稳态基础。项目不能以字段存在、局部单测、HTTP 成功或脚本成功退出代替完整能力证据；机器状态必须继续保持 `IN_PROGRESS`，直到所有必需验证层各自给出新鲜、匹配风险的结果。

本账本只记录当前开放责任。每条“事实”必须能由当前源码调用链、当前自动化失败/缺口、真实运行或批准设计的未完成 Exit Gate 证明；“待验证”只描述尚需最小复现或外层验收的推断，不得写成线上事故。访客文本、Prompt、模型原始输出、凭据、Token、handle 和私有路径不得进入本文。

## 2. 维护、编号、水位和删除规则

- 正式命名空间只有 `A2`、`ARCH`、`GATE`、`DOC`。`A2` 表示生产行为缺陷；其余三类分别表示架构、验证和文档治理工作。
- 水位只表示编号曾使用到的位置，单调递增、永不复用，不保存事项标题、状态或正文：
  - A2 已用至 A2-120
  - ARCH 已用至 ARCH-22
  - GATE 已用至 GATE-23
  - DOC 已用至 DOC-09
- `P0`—`P3` 只表示影响；`NOW`、`NEXT`、`LATER` 是唯一执行序。同一执行序先服从依赖，再按影响排序。
- 新项先提升对应水位，再分配下一编号。每个开放正式 ID 只能有一行；共同根因或门可以互相引用，但不能建立第二份总览表。
- 条目只有在生产修复、原失败路径、针对性回归、受影响全量门以及与风险匹配的 Browser、PostgreSQL、跨 JVM 或获授权 Provider 门都成立后，才从总览、正文和专属 Exit Gate 中删除。本文不保留完成索引、实施流水或第二账本；必要行为摘要写入项目演进日志，细节由 Git 与测试资产追溯。
- 证据变化时直接更新原行；只有风险路径而机制未闭合时标为“待验证”，并把最小复现放在专属门首位。

## 3. 稳态不变量

1. 每个业务概念只有一个生产权威；不恢复旧 Router、兼容桥、配置式双栈或运行时 fallback，回退只依赖整体版本。
2. Frontend 与 Backend 消费同一公开合同源；模型不得决定 Task、DAG、Provider 或扩大公开主体。
3. 同 requestId replay 只复用现有 TurnExecutionStore 权威，不建立第二结果缓存。Provider 派生正文不持久化，重放终局为 `REPLAY_BODY_NOT_RETAINED`；关键词/sentinel 检测只属于测试；Portfolio continuation handle 原样保留。
4. 加密不能替代“不持久化”。生产只按闭合来源分类与未知来源默认拒绝执行持久化政策。
5. 未进入唯一生产路径的实现不算完成；未运行的验证不得记为 `PASS`；一个验证层不能替代另一层。
6. 上游能力未就绪时，下游门必须报告 `NOT_READY`，不得以 skip、空场景、固定成功字段或脚本退出状态冒充能力通过。

## 4. NOW/NEXT/LATER 总览

**NOW** 先修复证据假绿、replay/Provider 授权与状态真实性，再处理会破坏会话、澄清、恢复、推荐约束投影和 Browser 可达性的现行生产路径。这里的直接风险优先于功能扩展。

**NEXT** 在上述权威和门稳定后，完成页面上下文、多轮语义、Portfolio 表达、General 语言/深度/Comparison、结构化输出边界以及相应 Frontend 正文验收。

**LATER** 只在上游生产链与验证入口可达后执行跨 JVM Browser 恢复、每家 Provider 独立稳定性矩阵、token/cost canary 和退役资产最终零引用清理。严重度不会改变这些依赖顺序。

## 5. 开放生产问题（A2）

| ID | 严重度 | 执行序 | 状态 | 当前证据 | 目标 | 修复边界/依赖 | 专属验证 / Exit Gate |
|---|---|---|---|---|---|---|---|
| A2-15 | P1 | NEXT | OPEN | 事实：页面重新进入只恢复会话身份与状态，不证明旧 requestId 终局被取回；快速新调用可掩盖原请求丢失。 定位：`frontend/src/features/agent/composables/useConversationResume.ts`、`frontend/src/features/agent/components/AgentWorkspace.vue`。 | 页面重新进入时可按原 requestId 恢复同一权威终局，不以新调用掩盖丢失。 | 复用现有 replay 权威，区分恢复与新执行；依赖恢复链和 Provider LIVE。 | Browser 记录同一 requestId、调用次数与终局；重新进入不得以新 requestId 冒充恢复。 |
| A2-20 | P1 | NEXT | OPEN | 事实：`GeneralDraftValidator` 与 `GeneralDraftValidatorTest` 已固定逐句中文和结构规则；活动 Provider 计划 §8.0.2 记录 Qwen v6 最小真实样本中 CONCISE、STANDARD、DETAILED、CONVERSATIONAL 各 1/1 成功。待验证：当前 Browser specs 没有直接断言 General 正文语言与结构；首个验证动作是在 packaged Browser 固定 General 样本中解析公开响应并逐项断言中文、section 结构和页面正文。定位：`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`、`backend/src/test/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidatorTest.java`、`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.0.2。 | 真实页面正文稳定使用简体中文，并保留可验证的结构与技术标识例外。 | 保持简体中文正文约束和技术标识例外，不增加重试或正文日志。 | 获授权 Provider 固定样本与 Browser 正文逐项通过中文、结构和隐私门。 |
| A2-21 | P1 | NEXT | OPEN | 事实：Goal 的 typed parameters 持有 depth，`SemanticPlanCompiler#compile` 将 `ProposedGoal#getParameters` 传入 `SemanticTaskParameters#getParameters`；`GeneralDraftValidator` 与 `GeneralDraftValidatorTest` 按 depth 固定总句数区间与语义 aspect 覆盖。活动 Provider 计划 §8.0.2 只记录三档最小真实样本各 1/1 成功，不证明 Browser 中用途、边界、权衡与误区的语义差异。待验证：当前 Browser specs 没有三档同题正文对照；首个验证动作是在 packaged Browser 对同一主题分别提交 CONCISE、STANDARD、DETAILED，并比较公开响应与页面正文的结构、篇幅和语义要点。定位：`backend/src/main/java/com/portfolio/agent/turn/planning/UserGoalProposal.java`（`UserGoalProposal.ProposedGoal#getParameters`、`UserGoalProposal.GeneralExplanationParameters#getDepth`）、`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticTaskParameters.java`（`SemanticTaskParameters#getParameters`）、`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticPlanCompiler.java`（`SemanticPlanCompiler#compile`）、`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`、`backend/src/test/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidatorTest.java`、`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.0.2。 | 三档 depth 在用户可见的用途、边界、权衡与误区上形成稳定差异。 | 由同一 depth 权威控制结构与篇幅；依赖 General 质量和 Browser 正文门。 | 三档输入在生产入口形成可断言的结构、篇幅和语义差异，Browser 无降档。 |
| A2-22 | P1 | NOW | OPEN | 事实：`AgentWorkspace.vue` 的 `TurnSubmissionSnapshot` 已冻结 requestId、modelSelection、command、surfaceContext、conversationWindow、resumeToken 与 displayQuestion，`retryFailure()` 把同一 submission 原样交回 `runTurn()`；待验证：原失败 Browser 路径尚无逐字段网络证据。 定位：`frontend/src/features/agent/components/AgentWorkspace.vue`。 | 失败重试始终复用首次提交快照与请求指纹。 | 重试原样复用 command、surface、preset/model selection 与 fingerprint；不得重建请求。 | 先在原失败路径捕获首次/重试请求并逐字段比较；再由单元与 Browser 断言无 fingerprint 冲突、无额外 Provider 调用。 |
| A2-23 | P1 | NOW | OPEN | 事实：`AgentTurnLifecycleService` 先调用 `reserveClarification()`，再把 `ClarificationSettlementMutation.consume` 交给 terminal settlement；Memory/PostgreSQL store 已校验 reservation owner，并有回滚、竞争与过期回收测试；待验证：真实 RESOLVE、取消和结算交错的外层原路径尚未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleService.java`、`backend/src/test/java/com/portfolio/agent/turn/lifecycle/TurnExecutionStoreContractTest.java`。 | Clarification reservation 只随成功 terminal settlement 原子消费。 | 消费只能随 terminal transaction 成功；依赖 TurnExecutionStore settlement。 | 先重放真实 RESOLVE 的取消/结算失败原路径；再以 Memory/PostgreSQL 竞争、重试和回收矩阵证明 reservation 可恢复且只消费一次。 |
| A2-24 | P1 | NOW | OPEN | 事实：`AgentTurnLifecycleService` 已把 `ENTER_RECOMMENDED_RESULT` 路由到直接进入、把 `NEEDS_CLARIFICATION` 路由到 `recommendationSelectionClarification`；`AgentTurnLifecycleContinuationTest#singleCandidateStillClarifiesWhenTheModelIsUncertain` 覆盖单候选；待验证：真实 Provider/Browser 原路径仍无发布证据。 定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleService.java`、`backend/src/test/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleContinuationTest.java`。 | 模型不确定的单候选保持限定澄清，明确进入意图才直接进入。 | 保持单一 Semantic Routing/Lifecycle 权威，不用前端推测状态。 | 先在真实单候选原路径确认限定澄清而非直接进入；解析后恢复原 Goal，明确 ENTER 仍可直接进入。 |
| A2-25 | P2 | NOW | OPEN | 事实：PostgreSQL Session replacement 与 expired discussion pointer 的 parity 仍在联合门中开放。 定位：`backend/src/test/java/com/portfolio/agent/turn/state/postgres/JdbcAgentStateStoreIntegrationTest.java`。 | Session replacement 在同一事务中移除全部旧 discussion pointer。 | replacement 必须在同一事务清理旧 pointer；依赖状态 schema 与 store 合同。 | PostgreSQL replacement、重启、过期和并发测试均无残留 pointer。 |
| A2-26 | P1 | NOW | OPEN | 事实：生产 ENTER 已不以 Recommendation 的过期时间裁剪 discussion；`AgentTurnLifecycleService#enterRecommendationCandidate` 把 `session.expiresAt` 传给 `ProjectDiscussionCoordinator#enter`，`ProjectDiscussionCoordinator#transition` 只取配置 TTL 与会话上界的较早者；`ProjectDiscussionCoordinatorTest#enterCopiesExactlyTheRecommendationResultsAndBuildsOverviewGoal` 用 2 分钟 Recommendation 与 20 分钟会话上界固定为 20 分钟 discussion。生产修复已落地，剩余 lifecycle/packaged Browser 外门未闭合；首个验证动作是从临近过期 Recommendation 走真实 ENTER 并比较 recommendation、discussion 与 session 三个过期时间。定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleService.java`、`backend/src/main/java/com/portfolio/agent/turn/continuation/ProjectDiscussionCoordinator.java`、`backend/src/test/java/com/portfolio/agent/turn/continuation/ProjectDiscussionCoordinatorTest.java`。 | 每次 ENTER 都获得 discussion 自身定义的完整有效期。 | Discussion TTL 由自身权威计算，来源只提供合法 continuation identity。 | 真实 lifecycle 与 packaged Browser 以临近过期 Recommendation 完成 ENTER，并证明 discussion 过期时间取自身 TTL 与 session 上界的较早者，不受 Recommendation 过期时间提前截断。 |
| A2-27 | P2 | NOW | OPEN | 事实：`deletePendingGeneration` 只删除同 sessionId、同 requestId 的 pending，`runTurn` 在结算副作用前校验 `ownsGeneration`；`AgentWorkspace.test.ts` 的“取消 A 后提交 B”矩阵覆盖 A 的迟到成功与迟到失败均不污染 B。生产修复已落地，剩余 retry 交错与 Browser 外门未闭合；首个验证动作是复现 A 取消、A 同 requestId 重试、B 接管后 A 迟到 settle 的组合并断言 B pending 保持。定位：`frontend/src/features/agent/components/AgentWorkspace.vue`（`deletePendingGeneration`、`runTurn`）、`frontend/src/features/agent/components/AgentWorkspace.test.ts`（“取消 A 后提交 B”矩阵）。 | 旧 generation 的异步清理永远不能改变新请求的 pending 状态。 | 清理必须同时匹配 sessionId、requestId 与 generation。 | cancel、同 requestId retry、B 接管与 A 迟到 settle 的交错矩阵及 Browser 路径均证明 B pending 不被旧 generation 清除。 |
| A2-28 | P1 | NOW | OPEN | 事实：Discussion summary、revision、TTL 与恢复动作虽有 typed 投影，完整冷恢复/Browser 行为尚未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/continuation/ProjectDiscussionCoordinator.java`、`frontend/src/features/agent/composables/useConversationResume.ts`。 | Discussion 的 summary、revision、TTL 与恢复动作跨刷新和重启保持同一服务端语义。 | 服务端 summary 是唯一权威；前端不得推算 revision 或过期动作。 | active/expired/entered 三态经刷新和重启后投影一致，动作只来自当前 summary。 |
| A2-30 | P0 | NOW | OPEN | 事实：Goal label 与 Discussion action 已有安全投影修复，但完整 settlement 外层门仍是关闭条件。 定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/PersistenceSafeReplayPolicy.java`、`backend/src/test/java/com/portfolio/agent/turn/state/postgres/AgentStatePayloadCodecTest.java`。 | 任何访客派生文本都不会进入可持久化 settlement。 | 禁止 visitor-derived text 进入 replay；加密不构成豁免。 | 固定输入标记贯穿 Goal/Projection/settlement，解密后的 publicTurn、contexts、challenges 均无原文片段。 |
| A2-31 | P0 | NOW | OPEN | 事实：General 与非快速 Conversational 正文来自 Provider；待验证：真实复述风险以 fixture 证明，不写成已发生。 定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/PersistenceSafeReplayPolicy.java`、`backend/src/test/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleReplayTest.java`。 | Provider 派生正文仅在首次响应可见，replay 只返回固定安全终局。 | Provider-derived text 默认 live-only；未知来源默认拒绝持久化。 | 复述 fixture 首次响应可见、持久化正文不可见，同 requestId 重放为固定安全终局。 |
| A2-32 | P0 | NOW | OPEN | 当前权威约束与推断（非运行事实）：本文 §3 固定 persistence-safe replay 标记，`backend/src/main/java/com/portfolio/agent/turn/lifecycle/PersistenceSafeReplayPolicy.java` 按正文来源分类；据此推断确定性 Portfolio 与 Provider 自由正文不能共享同一种精确 replay 语义。 | 确定性 Portfolio 与 Provider 自由正文分别遵守精确 replay 和不可保留语义。 | Portfolio 可精确 replay；Provider 正文只保存固定不可重放终局，不建第二缓存。 | 五种 PublicTurn variant 的首次、Memory/PostgreSQL replay、明文与调用次数矩阵通过。 |
| A2-33 | P0 | NOW | OPEN | 事实：当前 Turn 已显式冻结 ModelSelection，旧 global/provider-ref 权威已退出；两家真实接收方总门仍未完成。 定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnCommand.java`、`backend/src/main/java/com/portfolio/agent/infrastructure/model/ModelExecutionResolver.java`。 | 每个 Turn 只由冻结的 ModelSelection 指定唯一 Provider 接收方。 | Catalog、Provider policy、Operation binding 三层准入后冻结唯一接收方。 | 错配启动失败；显式选择只到指定 Provider fixture；两家获授权 canary 分别证明接收方。 |
| A2-35 | P1 | NOW | OPEN | 事实：公开 availability 消费共享 readiness，但真实 Provider 与 Frontend 消费总门仍开放。 定位：`backend/src/main/java/com/portfolio/agent/turn/infrastructure/AgentRuntimeReadiness.java`、`backend/src/main/java/com/portfolio/agent/portfolio/dto/response/AgentAvailabilityResponse.java`。 | 公开 availability 只反映当前确实可执行的冻结目录与 operation readiness。 | 可用性只投影已冻结、可执行的目录与 Operation readiness。 | 配置矩阵中公开目录、Bean wiring、首次请求和错误终局一致。 |
| A2-38 | P1 | NEXT | OPEN | 事实：同类 Claim 已合成为叙述块并聚合来源；真实 Browser 可读性和正文完整性未证明。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/presentation/PortfolioPresentationComposer.java`。 | Portfolio 回答以可读叙述块呈现，并让每个区块的公开来源可追溯。 | 只组合审核事实，不制造新事实；依赖 depth 与正文门。 | 每段来源闭合、区块数量符合 depth，Browser 正文非机械 Claim 列表。 |
| A2-39 | P1 | NEXT | OPEN | 事实：Goal 的 typed parameters 通过 `UserGoalProposal.PortfolioFactParameters#getDepth` 持有 depth，`SemanticPlanCompiler#compile` 将这些参数传入 `SemanticTaskParameters`，`PortfolioInvocationFactoryTest#portfolioOverviewDepthChangesRetrievalProfilesAndBounds` 固定 CONCISE/DETAILED 的检索差异；真实 Provider 选择与 Browser 差异仍待验收。定位：`backend/src/main/java/com/portfolio/agent/turn/planning/UserGoalProposal.java`、`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticPlanCompiler.java`、`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticTaskParameters.java`、`backend/src/test/java/com/portfolio/agent/turn/capability/portfolio/PortfolioInvocationFactoryTest.java`。 | 同一 depth 在 Goal 创建、澄清恢复和跨域 fan-out 中保持不变。 | depth 由 typed Goal 单权威持有并贯穿澄清与跨域子任务。 | 三档 Goal 在生产入口、恢复路径与 Browser 结果中保持一致。 |
| A2-40 | P1 | NEXT | OPEN | 事实：depth 已影响检索、候选上限、coverage 和区块；外层差异矩阵未完成。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/retrieval/RetrievalRequest.java`、`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/semantic/PortfolioSemanticResultFactory.java`。 | depth 对召回、覆盖、区块和完成状态产生一致且用户可见的差异。 | 禁止只加装饰字段；依赖 Portfolio retrieval/presentation 联合门。 | 同一主题三档输入产生可断言的召回、覆盖、区块和完成状态差异。 |
| A2-41 | P1 | NEXT | OPEN | 事实：typed parameters 已作为下游权威，requestedOutputs 由其派生并校验；真实合同门未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticTaskParameters.java`、`backend/src/main/java/com/portfolio/agent/turn/planning/GoalResolver.java`。 | AnswerIntent 只有 typed parameters 一个权威，其他字段均确定性派生。 | 只保留一个 AnswerIntent 权威，其他表示必须确定性派生。 | 互相矛盾的 outputs/facets 失败关闭，合法路径跨 Codec、Plan、Browser 一致。 |
| A2-43 | P1 | NEXT | OPEN | 事实：闭合 constraints 已进入召回、排序与缺口计算；真实 Provider/Browser 用户可见差异未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/semantic/PortfolioSemanticResultFactory.java`、`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/retrieval/postgres/selection/PostgresHybridCandidateRetriever.java`。 | 公开约束真实影响候选与排序，无法满足的约束对用户可见。 | 约束只能来自公开目录，目录外值失败关闭。 | 固定矩阵证明约束改变候选或排序，缺口经公开合同和 Browser 可见。 |
| A2-44 | P1 | NOW | OPEN | 事实：`PortfolioSemanticResultFactory` 在数量完整但有约束缺口时产出 `PARTIAL` 且 omissions 为空；`PublicAgentTurnProjector` 因 `PARTIAL` 伪造“公开结果数量不足”到 incompleteReasons；`PublicPresentation.Recommendation` 随后因数量完整而抛错，后面的 `gapNotices()` fallback 不可达；Frontend 还把 unsatisfiedConstraints 藏在 count-incomplete 条件内。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/semantic/PortfolioSemanticResultFactory.java`、`backend/src/main/java/com/portfolio/agent/turn/projection/PublicAgentTurnProjector.java`、`frontend/src/features/agent/components/RecommendationPresentationView.vue`。 | 数量完整但约束不足时返回合法 PARTIAL，并在页面直接展示约束缺口。 | 分离数量缺口与约束缺口；Projector 不得为约束缺口伪造数量理由。依赖 Backend invariant 与 Frontend 展示同时修复。 | Backend 原路径以完整数量+约束缺口返回合法 `PARTIAL` 和明确约束且不抛错；Browser 在数量完整时仍展示 unsatisfiedConstraints。 |
| A2-45 | P1 | NEXT | OPEN | 事实：每项已有闭合 reason code、固定公开说明与 publicSourceKeys；真实 Provider/Browser 总门未完成。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/presentation/PortfolioPresentationComposer.java`、`backend/src/main/java/com/portfolio/agent/turn/projection/PublicAgentTurnProjector.java`。 | 每个推荐理由只来自闭合 reason code 与实际公开证据。 | 理由只能由审核证据和闭合代码产生。 | 每个推荐项理由、来源和候选身份一致，缺来源或未知 reason 失败关闭。 |
| A2-46 | P1 | NEXT | OPEN | 事实：requestedSize、career track、capability 已参与召回与稳定排序；真实目标敏感矩阵仍开放。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/retrieval/BundlePortfolioRetrieverAdapter.java`、`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/retrieval/postgres/selection/PostgresHybridCandidateRetriever.java`。 | 同一 Goal 在 Bundle 与 PostgreSQL 中得到一致、可解释的目标敏感排序。 | 同一确定性排序权威同时覆盖 Bundle/PostgreSQL。 | 固定目标矩阵在两种数据源产生一致且可解释的候选/排序差异。 |
| A2-47 | P1 | NEXT | OPEN | 事实：Portfolio Comparison 已按 dimension 生成对齐 section；Browser 正文与真实 Provider 门未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/presentation/PortfolioPresentationComposer.java`。 | Comparison 按请求 dimension 对齐每个主体的差异、取舍、缺口和来源。 | 比较必须以闭合 dimension 对齐主体证据，不顺序拼接。 | 每个请求 pair 都有差异、取舍、缺口与来源，额外/遗漏 pair 失败。 |
| A2-48 | P1 | NEXT | OPEN | 事实：Codec 与 Invocation 已拒绝未知 comparison dimension；外层真实路径未验收。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/GoalProposalCodec.java`、`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/PortfolioInvocationFactory.java`。 | 未知 comparison dimension 在所有入口形成同一拒绝或澄清终局。 | 未知值只允许拒绝或澄清，不降级成其他 facet。 | Provider fixture、API 与 Browser 输入未知值均形成同一安全终局。 |
| A2-49 | P1 | NEXT | OPEN | 事实：Portfolio dimension 已改为后端闭合集合；真实 Provider/Browser 仍需证明消费。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticTaskParameters.java`、`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/PortfolioInvocationFactory.java`。 | 一个闭合 dimension 枚举贯穿 Goal、检索、coverage 与 presentation。 | 一个枚举贯穿 Goal、retrieval、coverage 与 presentation。 | 每个合法维度有生产消费者和反向测试，未知/重复值失败关闭。 |
| A2-50 | P1 | NEXT | OPEN | 事实：Cross-domain 关系段已由 General mechanism 和 Claim category 映射；真实语义质量未证明。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/synthesis/CrossDomainPresentationComposer.java`。 | Cross-domain 关系只由可追溯事实与 caveat 组成，不引入表达第二权威。 | 只组合验证过的 statement/Claim/caveat，不引入模型表达第二权威。 | 固定证据与真实 Provider 样本均能追溯概念—项目关系和适用边界。 |
| A2-51 | P1 | NEXT | OPEN | 事实：`SemanticPlanCompiler#compileCrossDomain` 从同一个 `ApplyConceptParameters#getDepth` 构造 General 与 Portfolio typed parameters；`SemanticPlanCompilerTest#crossDomainDepthPropagatesToBothSupportingTasks` 固定两路均为 DETAILED。生产 fan-out 已落地，Browser 联合门未完成。定位：`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticPlanCompiler.java`、`backend/src/test/java/com/portfolio/agent/turn/planning/SemanticPlanCompilerTest.java`。 | 跨域两路任务共享冻结 depth，并在综合结果中保持一致。 | depth 由上游 Goal 一次冻结，fan-out 不得各自改写。 | 三档跨域请求的两路任务、综合结果和 Browser 结构一致。 |
| A2-52 | P1 | NEXT | OPEN | 事实：详细 overview 缺必需 profile 时会 `PARTIAL`；用户可见缺口文案尚未完成 Browser 验收。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/portfolio/semantic/PortfolioSemanticResultFactory.java`。 | 证据不足的详细 overview 明确呈现安全缺口与 PARTIAL。 | 证据不足不得表现为完整；依赖 coverage 与投影。 | 缺证据 fixture 返回安全缺口和 `PARTIAL`，完整 fixture 才能 `COMPLETE`。 |
| A2-53 | P1 | NEXT | OPEN | 事实：闭合 Audience 已进入全部任务并影响 General/Portfolio；真实角色差异矩阵未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/api/request/AgentTurnRequestMapper.java`、`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticTaskParameters.java`。 | Audience 只改变表达优先级，不改变事实、主体范围或隐私边界。 | 角色只能改变表达/优先级，不扩大主体或证据范围。 | 四种角色在相同证据上有 typed、可见差异，scope 与隐私保持不变。 |
| A2-54 | P1 | NEXT | OPEN | 事实：验证后的 subjectHint 已投影为 defaultSubject；真实页面消费未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/GoalInterpretationInputFactory.java`。 | defaultSubject 只来自可信页面上下文和当前公开目录。 | hint 只能来自公开目录和可信 surface，访客不能注入任意 ID。 | 合法/无效 hint 的 API、Provider 与 Browser 正反例通过。 |
| A2-55 | P1 | NEXT | OPEN | 事实：STANDARD 单主体省略表达可由后端绑定 `SURFACE_HINT`；真实 Browser 指代门未完成。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/SemanticRouteValidator.java`。 | 省略主体时正确使用页面默认，显式合法主体仍可覆盖。 | 显式其他公开主体可覆盖默认；不得把默认变成锁定主体。 | 项目/案例页省略表达绑定当前主体，显式切换仍准确，非法主体失败。 |
| A2-56 | P1 | NEXT | OPEN | 事实：V7 typed semantic state 已原子加密保存安全短摘要；真实 Provider/Browser 多轮仍未证明。 定位：`backend/src/main/java/com/portfolio/agent/turn/continuation/ConversationSemanticStateProjector.java`、`backend/src/main/java/com/portfolio/agent/turn/state/postgres/AgentStatePayloadCodec.java`。 | 跨重启多轮状态只保存闭合语义身份，不保存正文或敏感句柄。 | 只保存闭合 Goal/section 身份，不保存正文、anchor、Prompt 或 handle。 | PostgreSQL 重启与 Browser 多轮证明状态可用且解密明文无敏感字段。 |
| A2-57 | P1 | NEXT | OPEN | 事实：Provider 必须返回匹配 recentReference.goalId 才注入 `RECENT_TURN`；当前没有真实 Provider/Browser 生成稳定性结果。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/GoalInterpretationInputFactory.java`、`backend/src/main/java/com/portfolio/agent/turn/planning/GoalProposalCodec.java`。 | 只有精确 recent goal 引用才能绑定上一轮语义。 | 仅显式、精确引用可绑定上一 Goal，不做静默主题继承。 | 正确/错误/遗漏 goalId 的 Provider fixture 与 Browser “进一步展开”路径通过。 |
| A2-58 | P1 | NEXT | OPEN | 事实：V7 保存公开 sectionId/sectionKind 并校验归属；Browser 区块指代未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/continuation/ConversationSemanticStateProjector.java`。 | section 引用始终属于所引用 Goal，跨 Goal 或过期引用失败关闭。 | section 引用必须属于所引用 Goal，正文不进入状态。 | “展开第 N 区块”命中正确 section；跨 Goal、过期或未知 ID 失败关闭。 |
| A2-59 | P1 | NOW | OPEN | 事实：`latestRecommendationReference` 已在最近 settled 终局不是 Recommendation 时停止回溯，`AgentWorkspace.test.ts` 覆盖非推荐 ANSWER 与非 ANSWER 终局后不携带旧 handle；但该函数会跳过无 turn 的失败 USER 消息，因此“Recommendation → 不相关请求失败 → 下一次 ASK”仍可能回溯旧 handle。生产修复仅部分落地；首个验证动作是以该三步序列捕获第三次请求的 `referenceContextHandle`。定位：`frontend/src/features/agent/components/AgentWorkspace.vue`（`latestRecommendationReference`）、`frontend/src/features/agent/components/AgentWorkspace.test.ts`（A2-59 两个定向用例）。 | Frontend 只随最新相关结果发送 Recommendation hint，跨话题与跨会话不残留。 | context routing 按最新相关公开结果与会话归属清理。 | “Recommendation → 不相关请求失败 → 下一次 ASK”、新会话及失败后续请求均不携带陈旧 `referenceContextHandle`，并由 Browser 网络 trace 证明。 |
| A2-60 | P1 | NOW | OPEN | 事实：Discussion 已生成 typed challenge 和 generation guard；真实 Provider→澄清→resolve 原路径未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/continuation/ProjectDiscussionCoordinator.java`、`backend/src/test/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleContinuationTest.java`。 | Provider 澄清、facet resolve 与最终正文共享同一 discussion pointer 和原 Goal。 | pointer 保持服务端权威；clarification 不重新解释访客文本。 | Browser 完成限定澄清、facet resolve、pointer 不变且返回预期正文。 |
| A2-61 | P1 | NOW | OPEN | 事实：active discussion 已提供闭合 facet choice，expired 仅允许重进；前端消费门仍开放。 定位：`backend/src/main/java/com/portfolio/agent/turn/continuation/ProjectDiscussionContext.java`、`frontend/src/features/agent/components/AgentWorkspace.vue`。 | 澄清卡片的选项、只读状态与恢复动作完全来自当前 typed summary。 | choice、template、binding 必须精确闭合。 | active/expired choice 渲染、提交、只读与恢复动作均来自服务端投影。 |
| A2-62 | P2 | NEXT | OPEN | 事实：Provider-derived Conversational 已有语言、长度、控制字符和连续复述校验；真实固定样本未完成。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/ConversationalMessageValidator.java`、`backend/src/test/java/com/portfolio/agent/turn/planning/DeterministicConversationBoundaryTest.java`。 | 开放社交正文在语言、长度、控制字符与复述边界内安全可用。 | 只做来源/结构验证，不以关键词清洗生产正文。 | 获授权中英文、超长、复述和控制字符样本按 closed reason 通过/拒绝。 |
| A2-63 | P1 | NEXT | OPEN | 事实：General Validator 已按 depth 强制 statement 句数；真实 Provider/Browser 质量门未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`、`backend/src/test/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidatorTest.java`。 | 三档 General 正文严格落入各自句数与结构边界。 | 句数是不变量，不只写入 Prompt。 | 三档 Provider 样本和 Browser 正文均落入相应句数与结构桶。 |
| A2-64 | P1 | NEXT | OPEN | 事实：Validator 已要求每句含中文并拒绝完整英文句；外层验收仍开放。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`、`backend/src/test/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidatorTest.java`。 | General 正文以简体中文为主体，同时允许必要技术标识。 | 允许技术标识，拒绝非技术完整英文正文。 | 固定正反例、真实 Provider 样本与 Browser 展示全部满足语言规则。 |
| A2-65 | P1 | NEXT | OPEN | 事实：DETAILED 要求闭合 aspect 精确覆盖，但 aspect 仍由 Provider 自报。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`。 | DETAILED 的每个 aspect 都由正文真实、具体地覆盖。 | 结构闭合不能代替正文语义；依赖抽样与人工可复核指标。 | 真实样本逐 aspect 对照正文，缺失、错标或空泛覆盖均失败。 |
| A2-66 | P1 | NEXT | OPEN | 事实：Validator 已要求实际 comparison pair 等于请求笛卡尔积；A2-119 仍暴露容量和身份缺口。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`、`backend/src/main/java/com/portfolio/agent/turn/infrastructure/model/GeneralProviderDraftCompiler.java`。 | Comparison 容量有确定边界，每个 pair 具有可校验身份且集合精确。 | 先修容量与 pair identity，再维持无额外/遗漏/重复 pair。 | 边界容量、乱序、重复、额外与遗漏 pair 的 Codec/Provider/Browser 矩阵通过。 |
| A2-67 | P2 | NEXT | OPEN | 事实：caveat 已校验 kind、中文、句数、长度与去重；相关性仍待真实正文抽样。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`。 | 每条 caveat 都直接对应当前主题的适用边界。 | caveat 必须对应当前主题边界，不得成为通用免责声明。 | 固定错题 caveat 被拒绝，真实样本的 kind/text 与主题可追溯。 |
| A2-68 | P1 | NEXT | OPEN | 事实：语言、句数、aspect、exact pair 核心门已进入运行时；完整 Provider/Browser 质量门未通过。 定位：`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidator.java`、`scripts/assert-live-general-answer-quality.ps1`。 | 运行时拒绝和验收报告使用同一套 General 质量规则。 | 生产 Validator 与验收规则同源，不放宽运行时换取通过率。 | 运行时拒绝分布、真实正文质量和 Browser 终局按相同规则独立报告。 |
| A2-69 | P1 | NOW | OPEN | 事实：`AgentWorkspace.vue#isTransientClarificationBusy` 与 `conversationWindowOf` 已排除 busy 临时终局；busy resolve 会把对应 USER 标为失败并跳过 busy AGENT，定向测试覆盖下一请求窗口不含该配对。生产修复已落地，剩余取消、重试、最终 settle 与 packaged Browser 网络证据未闭合；首个验证动作是在这三种交错后捕获下一 ASK 的 `conversationWindow` 并校验从 USER 开始且严格交替。定位：`frontend/src/features/agent/components/AgentWorkspace.vue`（`isTransientClarificationBusy`、`conversationWindowOf`）、`frontend/src/features/agent/components/AgentWorkspace.test.ts`（“reservation busy 终局不进入会话窗口”用例）。 | 可信 conversationWindow 始终从 USER 开始并保持严格角色交替。 | 临时终局不得进入可信 conversationWindow。 | 取消、重试和最终 settle 三种交错后的下一 ASK 均由 packaged Browser 网络 trace 证明 `conversationWindow` 从 USER 开始、严格交替且不含临时 busy 配对。 |
| A2-70 | P1 | NOW | OPEN | 事实：`AgentWorkspace.vue#runTurn` 在可恢复失败时回滚 optimistic consumed，`cancelTurn` 在取消时也回滚；`AgentWorkspace.test.ts` 覆盖取消与可恢复失败，`agent-final-contract.spec.ts` 覆盖成功后卡片为 `CONSUMED` 且表单消失。生产修复已落地，剩余双击并发与新鲜 packaged Browser 外门未闭合；首个验证动作是对同一 clarification 连续双击并捕获请求数、卡片状态及最终 settlement。定位：`frontend/src/features/agent/components/AgentWorkspace.vue`（`runTurn`、`cancelTurn`）、`frontend/src/features/agent/components/AgentWorkspace.test.ts`（澄清取消/可恢复失败用例）、`frontend/e2e/agent-final-contract.spec.ts`（`data-clarification-state="CONSUMED"` 断言）。 | 可恢复失败释放澄清卡，成功消费后才永久只读。 | 卡片状态服从当前 reservation/terminal 结果，不由旧 pending 推断。 | packaged Browser 证明同一 clarification 双击只产生一次执行，取消或可恢复失败后可重新提交，成功消费后永久只读。 |
| A2-71 | P1 | NOW | OPEN | 事实：`AudienceDialogue.vue` 已把界面明确标为 `HOMEPAGE PREVIEW`，每次 `executeSubmission` 都发送空 `conversationWindow`，定向测试断言窗口为空。生产修复已落地，剩余重复提问的 Browser 网络 trace 未闭合；首个验证动作是连续两轮捕获请求并确认文案为单轮预览、第二轮仍无历史窗口。定位：`frontend/src/features/audience/components/AudienceDialogue.vue`（`executeSubmission`、`HOMEPAGE PREVIEW`）、`frontend/src/features/audience/components/AudienceDialogue.test.ts`（空 `conversationWindow` 断言）。 | 首页明确提供单轮预览或真正连续的多轮会话，不再含糊宣称。 | 明确选择单轮预览或复用同一会话，不保留模糊宣称。 | UI 文案与网络 trace 一致；若为多轮则 conversationId/window/token 连续。 |
| A2-72 | P1 | NOW | OPEN | 事实：`AudienceDialogue.vue` 的 `FailedSubmission` 冻结原 requestId 与完整 command，`retryFailedSubmission` 原样重放；定向测试证明 PRESET 失败重试复用同一 requestId 与 PRESET 身份。生产修复已落地，剩余服务端 exactly-once/replay Browser 外门未闭合；首个验证动作是让首次响应在服务端提交后断链，再重试并核对 fingerprint、执行次数与终局。定位：`frontend/src/features/audience/components/AudienceDialogue.vue`（`FailedSubmission`、`retryFailedSubmission`）、`frontend/src/features/audience/components/AudienceDialogue.test.ts`（“Preset 失败重试原样复用同一 requestId”用例）。 | 首页失败重试复用原 requestId 和同一执行权威。 | 失败视图保存原提交快照并原样 replay。 | 重试沿用 requestId/fingerprint，原请求只执行一次并得到同一终局。 |
| A2-73 | P1 | NOW | OPEN | 事实：`FailedSubmission` 冻结 command、surface、question 与 modelSelection，Preset 重试由保存快照恢复，不从文案重建；定向测试逐字段证明 requestId、presetId、revision 与 surface 不退化为 `FREE_TEXT`。生产修复已落地，剩余错误 preset revision 的 fail-closed 与 Browser 外门未闭合；首个验证动作是冻结旧 revision 后更新 preset，再重试并捕获结构化拒绝及请求字段。定位：`frontend/src/features/audience/components/AudienceDialogue.vue`（`FailedSubmission`、`retryFailedSubmission`）、`frontend/src/features/audience/components/AudienceDialogue.test.ts`（A2-72/A2-73 PRESET 重试用例）。 | Preset 重试始终保留 presetId、revision、surface 与结构化命令身份。 | 保存 presetId、revision、surface 和 requestId，不从文案反推命令。 | Browser 逐字段证明 PRESET 失败/重试命令身份一致，并证明保存的错误 revision 由服务端失败关闭而不退化为 `FREE_TEXT`。 |
| A2-74 | P2 | NOW | OPEN | 事实：`newRequestId` 优先使用 `crypto.randomUUID`，fallback 以 `getRandomValues` 生成 RFC 4122 v4，无任一安全随机源则失败关闭；单测覆盖三条分支。生产修复已落地，剩余广域碰撞抽样、后端验收与真实 fallback 环境外门未闭合；首个验证动作是在缺 `randomUUID` 的受支持浏览器批量生成并提交标识，统计重复且确认后端全部接受。定位：`frontend/src/features/agent/api/requestId.ts`（`newRequestId`）、`frontend/src/features/agent/api/requestId.test.ts`。 | 所有受支持浏览器环境都生成符合后端合同且不碰撞的 UUID。 | 使用合规 UUID 实现或明确标记环境不支持。 | 模拟缺 API 环境，所有 conversation/request 标识仍通过后端校验且不碰撞。 |
| A2-75 | P2 | NOW | OPEN | 事实：`AgentWorkspace.vue#removeSession` 已对任意会话 token 调用 `clearConversation`，定向测试证明删除非当前会话会清理对应服务端会话且不影响活跃会话；但调用为 fire-and-forget，本地会话立即删除，清理失败后没有现成重试入口。生产修复仅部分落地；首个验证动作是让非当前会话的 clear 失败，观察本地 token 已移除且无法重试，再覆盖网络恢复与重启。定位：`frontend/src/features/agent/components/AgentWorkspace.vue`（`removeSession`）、`frontend/src/features/agent/components/AgentWorkspace.test.ts`（“删除非当前会话也 best-effort 清理”用例）。 | 删除任一本地会话都会精确清理对应服务端状态，并允许安全重试。 | 任意会话删除均 best-effort 调用精确服务端 clear，不能误清当前会话。 | 多会话删除、网络失败、重试与重启后目标会话状态符合预期。 |
| A2-76 | P2 | NOW | OPEN | 事实：`discussionExpirySyncTarget` 与其 watcher 会在倒计时归零后调用 `fetchCurrentConversation` 并采纳服务端 summary；定向测试覆盖无需 reload 取得 `EXPIRED`、首次冷取失败后重试及第二次 discussion 到期。生产修复已落地，但现有 expiry Playwright 仍显式 `page.reload()`，无 reload 的 packaged Browser 外门未闭合；首个验证动作是移除 reload，在页面停留越过 TTL 后断言 conversation GET 与唯一恢复动作。定位：`frontend/src/features/agent/components/AgentWorkspace.vue`（`discussionExpirySyncTarget` watcher、`fetchCurrentConversation`）、`frontend/src/features/agent/components/AgentWorkspace.test.ts`（A2-76/77 三个定向用例）、`frontend/e2e/agent-project-discussion-expiry.spec.ts`（现有 `page.reload()`）。 | Discussion 到期后无需 reload 即同步服务端 EXPIRED summary。 | 到期触发受控冷恢复，动作来自服务端。 | packaged Browser 在不 reload 的页面停留路径越过 TTL 后发起 conversation GET，取得权威 `EXPIRED` summary，并只展示服务端给出的合法恢复动作。 |
| A2-77 | P2 | NOW | OPEN | 事实：页面停留期间的到期同步已复用 conversation GET，定向测试覆盖首次失败后下个周期重试和同一会话第二次到期不被抑制；现有 expiry Playwright 仍靠 `page.reload()` 暴露恢复入口。生产修复已落地，剩余无 reload 的 packaged Browser 对等性外门未闭合；首个验证动作是在 active 页面停留至过期，比较自动同步与刷新后的 summary/action。定位：`frontend/src/features/agent/components/AgentWorkspace.vue`（到期 watcher、`fetchCurrentConversation`）、`frontend/src/features/agent/components/AgentWorkspace.test.ts`（“首次失败后重试”“第二次讨论到期”用例）、`frontend/e2e/agent-project-discussion-expiry.spec.ts`（`page.reload()` 路径）。 | 页面停留期间过期也能自动进入与刷新一致的恢复路径。 | 复用 conversation GET，不新增客户端状态权威。 | active 页面停留至过期后自动进入合法恢复路径，刷新前后一致。 |
| A2-80 | P1 | NEXT | OPEN | 事实：Qwen Comparison 在进入 General 前被 Goal Draft v1 阻断；Qwen/GLM 全能力独立矩阵均未完成。 定位：`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.0.2。 | 获批后 Qwen Comparison 能穿过 Goal 层到达 General，未获批时明确 NOT_READY。 | Goal v2 生产绑定是未批准 Level 3 决策，本项不得实施；先取得批准或保持 `NOT_READY`。 | 批准后原 Comparison 路径进入 General；两家分别完成 schema、semantic 和终局 canary。 |
| A2-82 | P2 | NEXT | OPEN | 事实：401/403、402、429、5xx、其他 4xx 已闭合分类；真实分布与公开映射仍未完整验证。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/OpenAiCompatibleStructuredModelTransport.java`、`backend/src/test/java/com/portfolio/agent/infrastructure/model/OpenAiCompatibleStructuredModelTransportProtocolTest.java`。 | Provider HTTP 状态在 fixture 与真实调用中映射到同一闭合诊断。 | diagnostics 只保留 closed layer/code，不保留 Provider body。 | 各状态 fixture 与获授权失败 canary 映射一致，日志无正文。 |
| A2-84 | P2 | NEXT | OPEN | 事实：Transport 已在读取期限制响应体并保留 absolute deadline；真实总门未完成。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/OpenAiCompatibleStructuredModelTransport.java`、`backend/src/test/java/com/portfolio/agent/infrastructure/model/OpenAiCompatibleStructuredModelTransportDeadlineTest.java`。 | 响应体在完整分配前受字节和 absolute deadline 双重边界保护。 | 上限在分配完整正文前生效，取消订阅且不 fallback。 | 边界字节、超一字节、body-stall、线程中断和真实大响应门通过。 |
| A2-85 | P1 | NEXT | OPEN | 事实：schema/semantic 拒绝已冻结为单次调用失败，不 repair、不重试。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/structured/StructuredOutputGateway.java`、`backend/src/test/java/com/portfolio/agent/infrastructure/model/structured/StructuredOutputGatewayTest.java`。 | 任何 schema、semantic 或 transport 失败都只调用 Provider 一次。 | 未来改变需独立隐私、成本、deadline 与质量批准。 | Goal/General schema、semantic、HTTP、JSON、envelope 失败均断言调用数严格为一。 |
| A2-86 | P1 | NEXT | OPEN | 事实：同 Turn 已冻结单一显式 ModelSelection；自动跨 Provider 重发被禁止，Frontend 切换体验仍未闭合。 定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnCommand.java`、`backend/src/test/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleSelectedModelFailureTest.java`、`frontend/e2e/agent-model-selection.spec.ts`。 | 用户手动换模型创建新 Turn，replay 永远保留原模型选择。 | 用户换模型只能新 requestId；普通 replay 保留原选择。 | 失败不触达第二 Provider；手动切换创建新 Turn，旧 requestId replay 不改模型。 |
| A2-116 | P1 | NEXT | OPEN | 事实：低信息确定性出口与 Provider Draft 已取得局部正例；原始 Qwen 两轮 Browser/packaged 路径仍未完成发布验收。 定位：`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.0.2。 | 低信息确定性出口与原两轮 Qwen 路径都在生产 Browser 中可达且语义正确。 | 保持窄出口、严格 blockedGoal、无 repair/retry/fallback；不借此启动未批准 Goal v2。 | 首轮模型调用为零，typed recent state/合法澄清不被拦截，原两轮 Qwen 路径通过。 |
| A2-117 | P1 | NEXT | OPEN | 事实：Goal 与 General 双合同编译层已进入生产；Qwen Comparison 仍被上游 Goal Draft v1 拒绝，Qwen/GLM 完整独立矩阵未完成。 定位：`backend/src/main/java/com/portfolio/agent/turn/infrastructure/model/GoalProviderDraftCompiler.java`、`backend/src/main/java/com/portfolio/agent/turn/infrastructure/model/GeneralProviderDraftCompiler.java`、`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.0.2。 | 获批的目标合同通过同一 fingerprint 驱动请求与校验，Comparison 可达且无 fallback。 | Goal v2 生产绑定是未批准 Level 3 决策；保持 frozen binding、单次调用和无 runtime fallback。 | 获批准后同一合同 fingerprint 驱动请求与本地校验；Comparison 可达；两家独立矩阵及全量/隐私/Browser 门通过。 |
| A2-118 | P0 | NOW | OPEN | 事实：网络/5xx 恢复可返回 `ok=false, invalid=false`，随后落入空 `ensureSession()`；`watchEffect` 会清除唯一 ResumeToken。待验证：仍需聚焦单元和 Browser 复现。 定位：`frontend/src/features/agent/composables/useConversationResume.ts`、`frontend/src/features/agent/components/AgentWorkspace.vue`。 | 瞬时恢复失败保留唯一 ResumeToken，并允许用户重试原会话。 | 瞬时失败必须保留 token 与可重试恢复状态；只有权威 invalid 才清除。 | 单元模拟 network/5xx，Browser 冷启动失败后重试恢复原会话；token 全程不丢失。 |
| A2-119 | P1 | NEXT | OPEN | 事实：合法 Goal 的 subjects×dimensions 可超过 General 的 20 项上限；Provider draft 句子又按排序后的 canonical pair 位置赋值，没有 pair identity。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/UserGoalProposal.java`、`backend/src/main/java/com/portfolio/agent/turn/infrastructure/model/GeneralProviderDraftCompiler.java`。 | Goal 容量与 General 容量一致，每个 Comparison pair 携带稳定可校验身份。 | 在 Goal 边界对齐容量，并让 draft 携带可校验 pair identity；依赖合同批准与 A2-66。 | 最大/超限组合在 Goal 层确定性处理；乱序/缺失/重复 pair 无法通过编译；Browser Comparison 对齐正确。 |

## 6. 架构工作（ARCH）

| ID | 严重度 | 执行序 | 状态 | 当前证据 | 目标 | 修复边界/依赖 | 专属验证 / Exit Gate |
|---|---|---|---|---|---|---|---|
| ARCH-01 | P1 | NOW | OPEN | 事实：批准设计要求 Domain Adapter 只依赖 `StructuredOutputGateway`，而 compiler 参数仍可由调用方传入。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/structured/StructuredOutputGateway.java`、`backend/src/main/java/com/portfolio/agent/turn/infrastructure/model/GoalInterpretationAdapter.java`。 | Domain Adapter 只能通过一个深 Gateway 方法完成结构化输出调用。 | Gateway 原子拥有 compile→单次 transport→extract→parse→schema validate；内部成员默认 package-private。 | 生产调用方只见一个 Gateway 方法且不能传 contract/compiler；架构依赖测试和零引用门通过。 |
| ARCH-02 | P0 | NOW | OPEN | 事实：`OperationBinding` 已冻结 Provider/Application 合同及 fingerprint，但精确 pair 与 consumer 一致性仍需总门。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/structured/OperationBinding.java`。 | OperationBinding 原子冻结精确合同对、策略、compiler、extractor 与 fingerprint。 | 两份合同、compiler、strategy、token policy、extractor 与 binding fingerprint 原子不可自由组合。 | 错配/缺失/未知组合启动前失败；Snapshot、请求、Gateway 与 canonical Codec fingerprint 一致。 |
| ARCH-03 | P1 | NOW | OPEN | 事实：`AgentRuntimeReadiness` 主要校验 operation policy/canonical contract，可执行 binding、目录和公开 availability 仍分层。 定位：`backend/src/main/java/com/portfolio/agent/turn/infrastructure/AgentRuntimeReadiness.java`、`backend/src/main/java/com/portfolio/agent/turn/infrastructure/AgentCapabilityConfiguration.java`。 | 启动期只有一个 Runtime Readiness 快照决定目录、binding 和公开 availability。 | 建立单一启动期 Runtime Readiness 快照，configured 不外推 schema/quality。 | 配置矩阵覆盖 agent、provider、binding、contract 与公开投影，任一缺口 fail-closed。 |
| ARCH-04 | P2 | NOW | OPEN | 事实：diagnostics 已有 closed layer/code，但 pointer、reason 所有权和重复发布边界仍需冻结。 定位：`backend/src/main/java/com/portfolio/agent/common/observability/ModelOutputDiagnostics.java`。 | 每次失败由唯一责任层发布一个安全、可定位的 pointer 与 closed reason。 | 每次失败只由责任层产生一个 closed reason 与安全 pointer，不暴露异常/正文。 | transport/gateway/domain/lifecycle 全链正反例证明唯一 reason、可定位且无重复计数。 |
| ARCH-05 | P1 | NEXT | OPEN | 可观察事实：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleService.java` 同时编排 claim、恢复、Goal、执行、结算与异常映射。架构推断：多职责集中会提高后续扩展成本；该成本判断尚无定量运行证据。 | Lifecycle 保留单一 façade，并以深模块隔离 claim、resolve、execute 与 settle。 | 保留唯一 lifecycle façade，内部按 claim/resolve/execute/settle 深模块拆分，不增加第二权威。 | 公共入口不变；模块依赖门无反向依赖；所有取消、幂等、原子结算合同通过。 |
| ARCH-06 | P1 | NEXT | OPEN | 事实：Frontend Workspace 聚合会话、pending、恢复、草稿、通知、来源和动作，多个现行缺陷共享此状态面。 定位：`frontend/src/features/agent/components/AgentWorkspace.vue`。 | Workspace 将会话、执行协调、恢复与展示拆为边界清晰的单一状态模块。 | 按 session store、turn coordinator、recovery、presentation 拆分，唯一 session/request 归属不变。 | 跨会话并发、取消、重试、恢复和删除测试无需组件私有状态互相修补。 |
| ARCH-07 | P0 | NEXT | OPEN | 事实：`TurnExecutionStore` 是终局权威，但 complete 参数和多种 mutation 仍可被调用方自由组合。 定位：`backend/src/main/java/com/portfolio/agent/turn/lifecycle/TurnExecutionStore.java`、`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnLifecycleService.java`。 | TurnExecutionStore 只接受闭合 settlement command，非法 mutation 组合不可表达。 | 引入闭合 settlement command，原子携带 PublicTurn、context/challenge/discussion/semantic-state mutation 与 guard。 | Memory/PostgreSQL 合同对所有合法/非法组合一致，部分提交、迟到完成和重复结算归零。 |
| ARCH-08 | P2 | NEXT | OPEN | 事实：多轮清理持续发现零消费者类型、配置和浅 record，缺少可执行纪律。 定位：`scripts/architecture-check.ps1`、`scripts/architecture-check.test.ps1`。 | 零消费者 record 与配置不能进入生产，安全删除有持续守卫。 | 新 record 必须有生产消费者和不变量；删除前证明无反射/序列化/注册入口。 | 架构检查阻止零消费者生产 record 与未消费配置；删除清单有零引用和 packaged 启动证据。 |
| ARCH-09 | P1 | LATER | OPEN | 事实：token field policy 已冻结在 binding，但缺真实 usage/cost canary，无法证明省略/上限策略的成本边界。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/structured/OperationBinding.java`、`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.2。 | 每个模型与 operation 的 token 字段、usage 和成本边界可用安全聚合 canary 验证。 | canary 只记录聚合 token/成本桶，不记录输入、输出或 Prompt；依赖真实 Provider 授权。 | 每模型/operation 验证请求 token 字段、响应 usage 与预算上限，超界阻止目录准入。 |
| ARCH-10 | P0 | NOW | OPEN | 事实：批准设计 §9 规定 Canonical Schema 是唯一 Provider wire-shape 权威；当前 General prompt 仍字面描述 JSON 形状，`GeneralProviderDraftCompiler` 仍维护 `ROOT_FIELDS`/`CAVEAT_FIELDS` 及数量/分支规则，`GeneralDraftCodec` 另维护 `ROOT_FIELDS`/`STATEMENT_FIELDS`/`CAVEAT_FIELDS` 与边界，存在重复知识。待验证的风险路径是 schema 变更后 compiler/prompt/codec 漂移；首个最小复现入口是对 Goal/General 的 schema property、enum、required 与 count 集合逐项 diff 这些常量和 prompt 字面量。定位：`docs/superpowers/specs/2026-08-25-provider-structured-output-capability-and-model-switching-design.md` §9、`backend/src/main/resources/prompts/general-provider-draft-system.txt`、`backend/src/main/java/com/portfolio/agent/turn/infrastructure/model/GeneralProviderDraftCompiler.java`、`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftCodec.java`。 | 每个 operation 只有一个 wire-shape 权威，所有其他表示均由其派生。 | 每个 operation 建立一个 wire-shape 权威，Prompt/request schema/compiler/Codec 只做派生或引用。 | 先扫描四类消费者并列出每个字段的当前 owner 与重复定义；再证明变更字段只改一处即可驱动生成/校验，故意漂移任一消费者时编译或合同门失败。 |
| ARCH-11 | P1 | NEXT | OPEN | 事实：零实现表达端口及幽灵预算已移除；最终零引用和风险对应外层门仍未合并。 定位：`scripts/architecture-check.ps1`、`scripts/code-quality-check.ps1`。 | 零实现表达路径保持物理删除，生产链不存在未批准替代入口。 | 保持物理删除，不重新接入未批准表达模型。 | 生产/测试/配置零引用，Backend、architecture、documentation 与 Browser 受影响门通过。 |
| ARCH-12 | P2 | LATER | OPEN | 事实：生产 runner 已迁移到当前 model-runtime/Provider/modelRef 配置，最终全仓零引用门仍需保留。 定位：`scripts/current-model-config-surface.test.ps1`。 | 退役 model/provider 配置在所有生产脚本与配置中保持零引用。 | 删除退役键，不提供兼容别名。 | 非测试脚本、生产配置与测试资产对退役前缀零引用，当前启动器自测通过。 |
| ARCH-13 | P2 | LATER | OPEN | 事实：无消费者的 Portfolio expression timeout 已删除；最终配置与文档零引用未统一收口。 定位：`scripts/architecture-check.ps1`、`scripts/code-quality-check.ps1`。 | Portfolio expression 不存在无消费者 timeout 或预算配置。 | 不在没有生产消费者时恢复预算。 | 配置绑定、环境变量、文档和测试零引用；预算关系只含真实 operation。 |
| ARCH-14 | P2 | LATER | OPEN | 事实：零入口的 Portfolio expression Port/Compiler 已物理删除；清理总门仍开放。 定位：`scripts/architecture-check.ps1`、`scripts/code-quality-check.ps1`。 | 未批准前生产与测试均不存在零入口 Portfolio expression Port/Compiler。 | 未批准前不重建表达器或可选构造分支。 | 生产/测试/Spring/反射零引用，architecture 与 packaged 回归通过。 |
| ARCH-15 | P2 | LATER | OPEN | 事实：无消费者 conversation history 配置已删除；最终零引用仍需合并发布门。 定位：`scripts/architecture-check.ps1`、`scripts/code-quality-check.ps1`。 | 无生产消费者的 conversation history 配置保持删除，未来需求先建立隐私设计。 | 若未来需要历史预算，先建立唯一消费者与隐私设计。 | properties、env、文档、绑定类和脚本零引用，ConversationWindow 隐私门不回归。 |
| ARCH-16 | P2 | LATER | OPEN | 事实：Operation readiness 已收敛为闭合真实枚举；文档和外层投影总门仍需统一。 定位：`backend/src/main/java/com/portfolio/agent/turn/infrastructure/AgentRuntimeReadiness.java`。 | readiness 只表达配置可执行性，不外推 schema 或质量结论。 | readiness 只说明配置可用，不宣称 schema/quality 已验证。 | 枚举零旧名，公开投影和文档不把 configured 推导成质量 PASS。 |
| ARCH-17 | P2 | LATER | OPEN | 事实：已识别的零消费者类型已删除；缺少持续的 record/dead-code 纪律。 定位：`scripts/architecture-check.ps1`、`scripts/architecture-check.test.ps1`。 | 每个生产 record 和配置都有真实消费者与明确不变量。 | 只在证明无生产入口、序列化或反射注册后物理删除。 | 零引用扫描、架构依赖测试和 packaged 启动共同证明删除安全。 |
| ARCH-18 | P2 | LATER | OPEN | 事实：Goal/General Codec 已启用 trailing-token 拒绝；双合同 Parser/Compiler 全链仍需一致门。 定位：`backend/src/main/java/com/portfolio/agent/turn/planning/GoalProposalCodec.java`、`backend/src/main/java/com/portfolio/agent/turn/capability/general/GeneralDraftCodec.java`。 | Provider Draft 与 canonical Codec 都拒绝 trailing token、重复键和多对象。 | 所有结构策略使用严格 Parser，不允许 Adapter 旁路。 | 合法对象后追加 token、重复键和多对象在 Provider Draft 与 canonical 层均失败。 |
| ARCH-19 | P1 | NOW | OPEN | 事实：Configured catalog 已收窄为配置与安全选择元数据；真实 Transport/Schema/Quality 仍由外部门证明。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/configuration/ConfiguredModelCatalog.java`、`backend/src/test/java/com/portfolio/agent/infrastructure/model/provider/ConfiguredModelCatalogTest.java`。 | Provider Registry 只陈述批准配置与请求特性，不宣称真实质量。 | 接口不得从 configured 推导 verified。 | Catalog API/测试无 `supports` 或质量强声明；真实层报告独立存在。 |
| ARCH-20 | P0 | NOW | OPEN | 事实：Operation schema 与 canonical contract registry 已做启动期校验；双合同与 Codec 的最终一致性仍需联合门。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/structured/OperationBinding.java`、`backend/src/main/java/com/portfolio/agent/infrastructure/model/structured/StructuredOutputContractRegistry.java`。 | Provider/Application 合同、compiler、fingerprint 与 Codec 始终原子一致。 | Provider/Application contract、fingerprint、compiler 与唯一 Codec 必须原子一致，无兼容 reader。 | 正确/错误/缺失合同启动矩阵和 provider-draft→canonical→Codec 正反例通过。 |
| ARCH-21 | P1 | NEXT | OPEN | 事实：GLM/Qwen 已有独立协议 Profile；两家真实 schema canary 未全部闭合。 定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/provider/ModelProviderProtocolProfile.java`、`backend/src/test/java/com/portfolio/agent/infrastructure/model/OpenAiCompatibleStructuredModelTransportProtocolTest.java`。 | GLM 与 Qwen 各自拥有隔离的协议 Profile，业务合同不泄漏到 Profile。 | Profile 只拥有 Provider envelope/thinking/stream 差异，不能泄漏业务合同。 | 两家 payload fixture 与获授权 canary 分别通过，任一字段变化不扩散另一家。 |
| ARCH-22 | P2 | NEXT | OPEN | 事实：Transport、JSON、envelope、schema、semantic 已分层；真实发生率与 pointer 可观测性仍开放。 定位：`backend/src/main/java/com/portfolio/agent/common/observability/ModelOutputDiagnostics.java`、`backend/src/test/java/com/portfolio/agent/turn/capability/general/GeneralModelOutputDiagnosticsTest.java`。 | 每次失败只产生一个责任层明确、可安全定位的 closed reason。 | 每次拒绝只由责任层发布一个 closed reason。 | 各层正反例和真实样本均产生唯一 layer/code，异常消息和正文零泄漏。 |

## 7. 验证治理（GATE）

| ID | 严重度 | 执行序 | 状态 | 当前证据 | 目标 | 修复边界/依赖 | 专属验证 / Exit Gate |
|---|---|---|---|---|---|---|---|
| GATE-02 | P1 | NOW | OPEN | 事实：Provider、Browser、共享合同、隐私与 persistence-safe replay 尚未形成同一发布证据链。 定位：`scripts/verify-release.ps1`、`docs/agent-architecture-status.json`。 | 发布结论由 Provider、Browser、合同、隐私与 replay 各层独立证据共同支撑。 | 各验证层独立报告，不能由单层绿色替代。 | 专项、合同、PostgreSQL、packaged Browser、隐私及获授权 Provider 门逐层有新鲜结果。 |
| GATE-03 | P1 | NOW | OPEN | 事实：scenario command 已接入生产 HTTP runner，但 expected、setup 和 hard-error trace 仍有失败/缺口。 定位：`scripts/run-agent-scenario-runtime.ps1`、`backend/src/test/java/com/portfolio/agent/turn/contract/AgentTurnScenarioManifestTest.java`。 | 每个 manifest 场景真实执行 setup、command、expected 与 hard-error trace。 | 每个场景必须执行真实 setup、command 和可观测断言。 | 全部 manifest 场景逐项运行并匹配 expected；缺 setup/trace 直接失败。 |
| GATE-04 | P1 | NOW | OPEN | 事实：`agent-project-discussion.spec.ts` 已解析响应正文，以 `expectAnswer`、`expectRecommendationItems`、`expectNonEmptyAnswer` 约束 kind、推荐项、goalResults、coverage 与 SECTIONED sections；`agent-model-selection.spec.ts` 也断言 ANSWER kind。Browser 正文语义断言已部分落地，剩余全部 happy path 的 resolution/正文质量负例与新鲜 packaged 执行外门未闭合；首个验证动作是按场景矩阵注入错误 kind、resolution、coverage 与空 section 并要求各 spec 失败。定位：`frontend/e2e/agent-project-discussion.spec.ts`（`turnBody`、`expectAnswer`、`expectRecommendationItems`、`expectNonEmptyAnswer`）、`frontend/e2e/agent-model-selection.spec.ts`（`expectAnswerKind`）。 | Browser happy path 同时证明 UI 状态与公开响应正文语义。 | 解析公开 body 并断言 kind、resolution、coverage。 | 每条 happy path 既断言 UI 也断言网络 body，错误 variant 必须使 spec 失败。 |
| GATE-05 | P1 | NOW | OPEN | 事实：公开响应不暴露可安全断言的 facet/depth trace，Browser 无法证明语义消费。 定位：`backend/src/main/java/com/portfolio/agent/turn/api/response/PublicAgentTurnResponse.java`、`frontend/e2e/agent-final-contract.spec.ts`。 | packaged 测试 lane 可观察闭合语义 trace，生产公开合同不暴露 trace。 | 只增加测试可见、脱敏且闭合的 trace；不得进入公开生产合同。 | packaged 测试 lane 能断言 facet/depth/subject，生产 lane 无 trace 泄漏。 |
| GATE-06 | P1 | NOW | OPEN | 事实：`expectNonEmptyAnswer` 已拒绝空 goalResults、`NONE` coverage 与空 SECTIONED sections；但尚未检查 section 文本非空、最小句数/depth 或来源，也没有对应的注入负例矩阵。待验证的风险路径是 HTTP 200 ANSWER 携带空白 section、单句详细回答或缺来源仍通过；首个最小复现入口是在 Playwright route/fixture 注入这些 variant 并直接调用同一 happy-path 断言。定位：`frontend/e2e/agent-project-discussion.spec.ts`（`expectNonEmptyAnswer`）。 | 空白、过短、缺 section、缺来源或 coverage 不足的正文不能通过 Browser 门。 | 正文质量门检查 section、来源、数量、coverage 与非空。 | 先把空白、单句、缺 section 与数量不足 fixture 逐类送入 Browser 断言并记录结果；再让全部负例失败、合法正文通过。 |
| GATE-07 | P1 | NOW | OPEN | 事实：部分 live gate 强字段已移除，但所有输出仍需只来源于实际观测。 定位：`scripts/assert-live-goal-draft-matrix.ps1`、`scripts/assert-live-general-answer-quality.ps1`。 | LIVE 报告的每个字段都能回溯到实际公开响应或 closed diagnostics。 | 不输出硬编码 goalKind 或未观测成功字段。 | 元测试注入不一致响应时 runner 报错；报告字段可逐项回溯公开响应或 closed diagnostics。 |
| GATE-08 | P1 | LATER | OPEN | 事实：packaged API 已证明跨两个 JVM 恢复，仍缺同一浏览器会话跨真实后端重启。 定位：`scripts/run-packaged-jvm-restart-api-gate.ps1`、`frontend/e2e/support/agent-recovery.ts`。 | 同一浏览器会话跨真实后端 JVM 重启后恢复 Conversation、state 与 replay。 | 复用同一 PostgreSQL、密钥、ResumeToken 与 Browser context。 | Browser 不重建会话跨后端重启后恢复 Conversation、typed state 和合法 replay。 |
| GATE-09 | P1 | NEXT | OPEN | 事实：General 单测已有语言、句数、depth、顺序负例；真实 Provider 质量矩阵仍开放。 定位：`backend/src/test/java/com/portfolio/agent/turn/capability/general/GeneralDraftValidatorTest.java`、`scripts/assert-live-general-answer-quality.ps1`。 | 固定负例与每家真实 Provider 样本使用同一 General 判定器。 | fixture 不能只自造满足规则的正文；依赖外部样本。 | 错误语言/句数/depth/order 正反例与每家真实抽样使用同一判定器。 |
| GATE-10 | P1 | NOW | OPEN | 事实：已存在 Lifecycle→PostgreSQL→解密完整 settlement 的运行时隐私门；发布组合尚未固定执行。 定位：`backend/src/test/java/com/portfolio/agent/turn/state/postgres/JdbcAgentStateStoreIntegrationTest.java`、`scripts/verify-release.ps1`。 | canonical 发布门固定执行完整 settlement 的运行时隐私检查。 | 静态 privacy scan 不能替代运行时数据流。 | canonical 发布门必须执行完整 settlement 标记测试，缺失或 skip 即失败。 |
| GATE-11 | P1 | NOW | OPEN | 事实：Codec 测试已扫描解密后的 publicTurn、contexts、challenges；最终五 variant 和回读联合门仍开放。 定位：`backend/src/test/java/com/portfolio/agent/turn/state/postgres/AgentStatePayloadCodecTest.java`。 | 全部 PublicTurn variant 在 Memory 与 PostgreSQL 回读后都经完整明文递归扫描。 | 扫描完整 plaintext 对象，不只扫描 resume template。 | 五 variant Memory/PostgreSQL 回读后递归扫描，任一字段含 visitor/provider 标记即失败。 |
| GATE-12 | P1 | NOW | OPEN | 事实：release summary 可包含 `FAILED`/`NOT_RUN`，而 runner 因未传 `-RequireComplete` 仍退出 0。 定位：`scripts/verify-release.ps1`、`scripts/write-agent-verification-summary.ps1`。 | canonical release 对任何 FAILED、NOT_RUN 或必需层缺失都以非零退出。 | 发布入口必须把分层状态转成进程失败；禁止用退出 0 代替具体 gate 证据。 | fixture 令任一必需层失败/未运行时 canonical release 非零退出；全部层真实 PASS 才退出 0。 |
| GATE-13 | P2 | NOW | OPEN | 事实：测试总量不能表达用户场景和风险覆盖；当前分层报告仍需成为唯一发布口径。 定位：`scripts/write-agent-verification-summary.ps1`、`scripts/write-agent-verification-summary.test.ps1`。 | 发布覆盖只按场景、风险和执行状态表达，不再用测试总量代替。 | 只按场景、风险门与执行状态报告覆盖，不搬运总量。 | 报告逐场景列出 matched/setup/trace 与风险层，新增无断言测试不提升完成状态。 |
| GATE-14 | P2 | NEXT | OPEN | 事实：后端 suggested-question 数量伪权威已删除；Frontend 展示数量和交互验收仍开放。 定位：`frontend/src/features/audience/components/AudienceDialogue.vue`、`frontend/src/features/agent/components/AgentWorkspace.vue`。 | suggested-question 数量只由当前 Frontend 设计权威决定。 | 数量由唯一前端设计/发布 preset 权威决定，不重建后端无消费者配置。 | 当前权威唯一可检索，桌面/移动 Browser 数量、布局和交互通过。 |
| GATE-15 | P0 | NOW | OPEN | 事实：`scripts/persistence-safe-replay-docs-check.ps1` 已显式按 UTF-8 读取五个目标文档，配套 test 在 Windows PowerShell 5.1 与 PowerShell 7 分别覆盖正例和缺 token 负例；编码门已经关闭。当前开放责任是 `docs/agent-architecture-status.json` 所述运行时 privacy 规则与五种 PublicTurn 的持久化分类持续一致，不再属于 checker 编码问题。 定位：`scripts/persistence-safe-replay-docs-check.ps1`、`docs/agent-architecture-status.json`。 | 文档、Codec、settlement 与机器状态共享一套 persistence-safe replay 分类。 | AGENTS、SECURITY、当前状态、机器状态、Codec 与 settlement 测试使用同一允许/禁止分类；不恢复已经完成的 GATE 工作。 | 五种 PublicTurn 的首次响应、Memory/PostgreSQL 回读与同 requestId replay 同时验证三条安全标记和正文来源分类；文档 marker 漂移仍由双宿主回归拒绝。 |
| GATE-16 | P1 | NOW | OPEN | 事实：`verify-release.ps1` 未传 `-RequireComplete`，所以含 `FAILED`/`NOT_RUN` 的汇总仍可随命令退出 0；机器证据不能据此宣称 PASS。 定位：`scripts/verify-release.ps1`、`scripts/write-agent-verification-summary.test.ps1`。 | 机器证据只接受具体新鲜 gate，release summary 的退出状态不能代替。 | EVIDENCE hard invariant 只接受五类新鲜具体 gate，不接受汇总命令成功。 | 负例让任一层缺失时 release 与 architecture checker 均失败；机器状态不得升级。 |
| GATE-17 | P2 | NOW | OPEN | 事实：刷新、PostgreSQL、跨 JVM API、同浏览器跨 JVM 已分层，但最后一层仍未运行。 定位：`frontend/src/features/agent/composables/useConversationResume.ts`、`scripts/run-packaged-jvm-restart-api-gate.ps1`。 | 刷新、PostgreSQL、跨 JVM API 与同浏览器跨 JVM 恢复各自独立留证。 | 每类恢复独立留证，不互相替代。 | 汇总分别报告四类状态；缺任一必需层不得整体 PASS。 |
| GATE-18 | P1 | NOW | OPEN | 事实：机器状态中的 `EVIDENCE_BEFORE_COMPLETION` 仍为 `FAILED`，明确列出 Browser 正文语义、同一浏览器跨重启与 Provider 矩阵缺口；`docs/16` §7 也规定各层证据不得互相替代。待验证的风险路径是活动计划或发布摘要仍以代码存在/局部单测宣称能力完成；首个最小复现入口是把 `docs/08`、活动计划与发布摘要中的完成表述逐项映射到“生产消费者、用户可见差异、负例、全链门”四列，任一缺项保持开放。定位：`docs/agent-architecture-status.json`（`EVIDENCE_BEFORE_COMPLETION`）、`docs/16-Agent单权威持续收敛范式.md` §7。 | 任何能力完成结论同时具备生产消费、用户可见差异、负例和全链证据。 | 完成必须同时具备生产消费、用户可见差异、负例和全链门。 | 先扫描当前状态、活动计划与发布报告中的能力完成表述并建立四类证据矩阵；任一缺项保持开放。 |
| GATE-19 | P1 | NOW | OPEN | 事实：packaged runner 未启用 `PLAYWRIGHT_MODEL_SELECTION`；公开可选模型不足两项时 spec 还会 skip。 定位：`scripts/run-jar-e2e.ps1`、`frontend/e2e/agent-model-selection.spec.ts`。 | packaged Browser 拥有可达的双模型切换 lane，前置不足时明确失败。 | 增加明确 packaged lane 和双模型 fixture；skip 不能记为覆盖。 | runner 元测试证明 env 设置/恢复和 spec 可达；lane 必须执行两模型切换，前置不足时报 `NOT_READY`/失败。 |
| GATE-20 | P1 | NOW | OPEN | 事实：`agent-project-discussion.spec.ts` 已通过 `expectAnswer` 拒绝非 `ANSWER` 终局，并由 `expectNonEmptyAnswer` 校验 goalResults、coverage 与 SECTIONED sections；`agent-model-selection.spec.ts#expectAnswerKind` 也拒绝错误 kind。Browser 场景判定已部分落地，剩余所有 happy path 的 resolution/body 分类与新鲜 packaged 执行外门未闭合；首个验证动作是向每个场景注入 HTTP 200 的错误 PublicTurn variant 并要求对应 spec 非零。定位：`frontend/e2e/agent-project-discussion.spec.ts`（`expectAnswer`、`expectNonEmptyAnswer`）、`frontend/e2e/agent-model-selection.spec.ts`（`expectAnswerKind`）。 | HTTP 200 只有在 PublicTurn kind、resolution 与 body 符合场景预期时才算成功。 | happy path 必须断言预期 PublicTurn kind/resolution/body。 | 每个 Browser 场景拒绝错误终局，失败 variant 只进入对应错误体验。 |
| GATE-21 | P1 | LATER | OPEN | 事实：两家成功调用的稳定 P50/P95 与跨端预算基线不足，失败样本不能冻结预算。 定位：`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.2—§8.3。 | 只有稳定成功样本才能形成每家 Provider 的延迟与预算基线。 | 依赖可达且通过基本 schema/semantic 的独立矩阵。 | 每家分 operation 报告成功延迟分布与超时率，再验证 operation<Turn<client<gateway<lease。 |
| GATE-22 | P1 | LATER | OPEN | 事实：Qwen Comparison 被上游 Goal Draft v1 阻断；Qwen/GLM 完整独立矩阵均未完成。 定位：`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.0.2、§8.2。 | Qwen 与 GLM 各自独立执行、失败和报告，互不替代。 | 依赖 A2-80 的批准决策与各自可用性；不得用一家结果代替另一家。 | runner 对每家独立执行、独立失败、独立报告，任何 `NOT_READY` 不算 PASS。 |
| GATE-23 | P1 | LATER | OPEN | 事实：Qwen Comparison 在到达 General 前被 Goal Draft v1 阻断；Qwen 与 GLM 的完整独立 General/Comparison 矩阵都尚未完成，现有局部样本不足以形成任一家稳定性结论。 定位：`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md` §8.0.2、§8.3。 | 每家 Provider 只以自身完整成功率、语义、schema、超时与延迟形成稳定性结论。 | 依赖 A2-80 的上游可达性与两家独立矩阵；不以一次通过或混合样本聚合。 | 上游解除阻断后，每家独立报告成功终局率、语义正确率、schema 拒绝率、超时率及 P50/P95。 |

## 8. 文档治理（DOC）

| ID | 严重度 | 执行序 | 状态 | 当前证据 | 目标 | 修复边界/依赖 | 专属验证 / Exit Gate |
|---|---|---|---|---|---|---|---|
| DOC-01 | P1 | NOW | OPEN | 待验证：当前未发现 `docs/08` 与本账本的确定冲突；批准计划仍要求逐项核对当前模型目录、四层恢复状态和各验证层证据口径，现有 documentation checker 不证明语义完全一致。 | docs/08 与开放账本对模型目录、恢复层级和验证状态保持同一当前口径。 | 只写当前实现与新鲜证据，不复制实施流水。 | 先对 `docs/08` 与 A2/ARCH/GATE 开放边界做逐项 diff；再由 documentation checker 证明状态、权威和恢复层级一致。 |
| DOC-02 | P2 | NOW | OPEN | 事实：`docs/11` 的当前 Provider 双合同演进条目仍记录 Backend/Frontend 测试总量与构建结果，与文件头“详细测试记录由 Git/报告承担”的纪律不一致。 定位：`docs/11-项目演进日志.md`。 | docs/11 只记录行为和边界，不保存测试数量、哈希或提交元数据。 | 演进日志记录行为、边界与治理裁决，不记录测试总量、制品哈希或提交元数据。 | 先扫描并分类测试计数、哈希与提交元数据；治理条目只保留行为摘要、责任边界和账本链接。 |
| DOC-03 | P1 | NOW | OPEN | 事实：documentation checker 已注册 audience-role switching UI 设计及 frontend UI 实施计划为活动文档，而 `docs/00` 的当前活动设计清单未列出这两个入口，活动集合并非双向同源。 定位：`scripts/documentation-check.ps1`、`docs/00-文档状态索引.md`。 | docs/00 与 documentation checker 共享同一活动文档集合和状态语义。 | 状态索引与 checker 共用明确活动集合；CURRENT_AUTHORITY/APPROVED/ACTIVE 语义一致。 | 先双向比较 `docs/00` 链接与 checker allowlist；任一缺项、路径失效或状态冲突必须非零退出。 |
| DOC-04 | P0 | NOW | OPEN | 事实：`SECURITY.md` 仍写“每个启用 Operation 的 `providerRef` 必须等于唯一 Transport 的 `ModelProviderKind`”，而当前 Turn 由 `AgentTurnCommand.ModelSelection` 显式选择，`ModelExecutionResolver#resolve` 解析，Profile 通过 `ApprovedModelExecutionProfile#getOperationBindings` 持有 Operation binding，旧权威表述尚未清理。待验证风险还包括单 Provider、无 retry/fallback 与 persistence-safe replay 的其余段落漂移；首个最小复现入口是对上述三个代码入口做逐句语义 diff，再扫描退役 global/provider-ref 术语。定位：`SECURITY.md`（Provider operation 约束段）、`backend/src/main/java/com/portfolio/agent/turn/lifecycle/AgentTurnCommand.java`（`ModelSelection`）、`backend/src/main/java/com/portfolio/agent/infrastructure/model/ModelExecutionResolver.java`（`resolve`）、`backend/src/main/java/com/portfolio/agent/infrastructure/model/configuration/ApprovedModelExecutionProfile.java`（`getOperationBindings`）。 | SECURITY 只描述当前 ModelSelection、单 Provider、无 fallback 与 replay 边界。 | 安全文档只描述当前目录、显式选择、单 Provider、无 retry/fallback 与 persistence-safe replay。 | 先对 SECURITY 与当前 ModelSelection/OperationBinding 做语义 diff 和退役权威扫描；再以安全 token 与代码/机器状态正反例确认一致。 |
| DOC-05 | P1 | NOW | OPEN | 事实：`docs/agent-architecture-status.json` 的 `updatedAt` 仍为 2026-08-24，部分 hard-invariant evidence 也固定在该日，而当前开放账本已更新；现有 checker 通过未证明证据新鲜度随账本变化重算。 定位：`docs/agent-architecture-status.json`、`scripts/agent-architecture-status.ps1`。 | 机器状态的日期、证据和总体状态随当前风险证据诚实更新。 | 每个 hard invariant 绑定新鲜、具体、风险匹配的证据；整体保持 `IN_PROGRESS`。 | 先以陈旧日期/缺证据负例证明 checker 会失败；再刷新并逐项复核 hard invariant，整体不得提前升级。 |
| DOC-06 | P2 | NEXT | OPEN | 事实：生产枚举 `TokenFieldPolicy` 只有 `MAX_TOKENS`、`OMIT`，活动 Provider 设计 §15 却列出 `OMIT_FOR_STRUCTURED_OUTPUT`、`SEND_MAX_TOKENS`、`SEND_MAX_COMPLETION_TOKENS`，活动计划又使用泛称 `token field policy`，命名存在漂移。待验证的风险路径是文档读者把未实现枚举当成生产能力；首个最小复现入口是分类活动文档每个 token-policy 命中，并与 `ApprovedModelExecutionProfile` 和协议 fixture 的实际字段投影逐项对照。定位：`backend/src/main/java/com/portfolio/agent/infrastructure/model/structured/TokenFieldPolicy.java`、`docs/superpowers/specs/2026-08-25-provider-structured-output-capability-and-model-switching-design.md` §15、`docs/superpowers/plans/2026-08-25-provider-structured-output-capability-and-model-switching.md`、`backend/src/main/java/com/portfolio/agent/infrastructure/model/configuration/ApprovedModelExecutionProfile.java`、`backend/src/test/java/com/portfolio/agent/infrastructure/model/OpenAiCompatibleStructuredModelTransportProtocolTest.java`。 | 代码与活动文档统一使用闭合 `TokenFieldPolicy` 名称。 | 一个闭合 `TokenFieldPolicy` 名称贯穿代码和文档，Provider 字段由策略派生。 | 先扫描并分类所有命名与退役别名；再由两家 payload fixture 证明 Provider 字段只从 `TokenFieldPolicy` 派生。 |
| DOC-07 | P1 | NOW | OPEN | 事实：当前删除 ID 扫描只命中明确历史标注及治理命令中的删除集合；`documentation-check.ps1` 虽区分 active/historical 文档，却没有持续校验删除 ID 映射的规则。待验证的风险路径是新的活动文档死引用仍能通过 documentation gate；首个最小复现入口是在临时 active-doc fixture 写入一个已删除 ID，并要求 checker 非零。定位：本文 §2、`scripts/documentation-check.ps1`（`$activeWorkArtifactStatuses` 与活动状态检查）。 | 活动文档中的历史 ID 引用都有明确历史语义或当前映射，不形成死入口。 | 活动文档中的旧引用必须删除、映射到当前开放项或明确标为历史语义；非活动材料不改写。 | 先扫描全部活动 specs/plans 并要求每个命中可分类；随后保证 CURRENT_AUTHORITY 文件无死引用、未标历史引用使检查失败。 |
| DOC-08 | P0 | NOW | OPEN | 事实：机器状态当前以 complete-settlement 测试记录隐私 `PASS`；整体证据不变量仍为失败。 定位：`docs/agent-architecture-status.json`、`scripts/agent-architecture-status.ps1`。 | 机器隐私 PASS 始终绑定新鲜、完整且匹配风险的数据流证据。 | 任何 PASS 必须引用新鲜完整数据流门；依赖状态 checker 和 replay 文档一致性。 | 删除或陈旧化完整 settlement 证据的负例必须使 checker 失败；新鲜正例才允许维持 PASS。 |
| DOC-09 | P1 | NOW | OPEN | 事实：Discussion 计划已区分 State/Lifecycle 与 Semantic Quality，但活动文档仍需防止完成表述回流。 定位：`docs/superpowers/plans/2026-08-21-agent-failure-recovery-and-discussion-completion.md`。 | 活动计划按责任层诚实表达状态，语义或 Browser 未过时不写完成。 | 计划状态按责任层拆分，语义/Browser 未通过不得写 Complete。 | documentation checker 对活动计划的过强完成词有负例，当前状态与账本一致。 |

## 9. 固定执行批次

1. **证据与账本先行：** 维持运行时 replay 分类一致性、修复 release 非零退出、机器状态新鲜度、活动文档引用和 Browser/spec 可达性。此批不改变生产能力。
2. **隐私、授权与结算权威：** 收口 persistence-safe replay、Provider/Binding/Readiness、Clarification reservation 和 settlement command；任何隐私或接收方错配先 fail-closed。
3. **现行交互链：** 修复 Recommendation 约束投影、会话/澄清/pending/retry/expiry、恢复 Token 与真实 scenario/Browser body。
4. **语义与表达：** 完成 Audience、subjectHint、多轮引用、Portfolio depth/Comparison/Cross-domain、General 语言/深度/caveat/pair identity。
5. **外层稳定性与清理：** 获得必要批准后执行 Qwen/GLM 独立矩阵、跨 JVM Browser、token/cost canary，最后完成退役资产零引用。Goal v2 未获 Level 3 批准前，其下游明确报告 `NOT_READY`。

## 10. 全局 Exit Gates

1. **ID 与文档结构：** 四个水位不倒退；每个开放 ID 恰有一行且字段齐全；无完成索引、实施流水、制品哈希、测试总量或第二账本；`CURRENT_AUTHORITY` 标记保留。
2. **隐私与 replay：** 首次响应、Memory/PostgreSQL settlement、同 requestId replay 覆盖全部 PublicTurn variant；visitor/provider-derived text 不落盘；三个安全标记由 Windows PowerShell 5.1 与 PowerShell 7 的 canonical 正反例保护。
3. **单权威：** Command、Goal、Plan、Execution、Projection、TurnExecutionStore、ModelSelection、OperationBinding、Runtime Readiness 和公开合同各只有一个生产权威；退役路径与兼容桥零引用。
4. **Backend：** 原失败路径、针对性单元/集成、模块架构、全量、Testcontainers、privacy、deadline/cancel/settlement 竞态门分别通过。
5. **Frontend：** 会话归属、pending、澄清、重试、恢复、来源、推荐缺口、正文质量和模型切换均有单元、类型/build 与无 warning 证据。
6. **Scenario 与 packaged Browser：** manifest 的 setup/command/expected/hard-error trace 实际执行；桌面与移动断言公开 body；同一浏览器跨真实后端重启恢复；skip 或 `NOT_READY` 不算 PASS。
7. **Provider：** 仅在明确授权后，每个批准 Provider 独立执行 Goal、General、Comparison、Clarification 与受控慢响应；报告 schema/semantic/终局/超时/延迟与安全聚合成本，不记录正文。一次成功或另一家的结果不能替代。
8. **发布与状态：** canonical release 对任一 `FAILED`、`NOT_RUN`、`IN_PROGRESS` 或必需层缺失均非零退出；architecture、documentation、privacy、replay 专项和 `git diff --check` 通过；机器状态仍为 `IN_PROGRESS`，直至所有硬不变量及开放责任真正完成。
