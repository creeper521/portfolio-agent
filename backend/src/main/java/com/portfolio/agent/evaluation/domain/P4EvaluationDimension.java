package com.portfolio.agent.evaluation.domain;

/** P4 evaluation surfaces. Real-provider dimensions are never implied by mock runs. */
public enum P4EvaluationDimension {
    OFFLINE_VALIDATION,
    MOCK_PROVIDER_INTEGRATION,
    PRIVACY_CAPTURE,
    MODEL_CONFORMANCE,
    REAL_PROVIDER_ACCEPTANCE,
    ANSWER_QUALITY_COMPARISON
}
