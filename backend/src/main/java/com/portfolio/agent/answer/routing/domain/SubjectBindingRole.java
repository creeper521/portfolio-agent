package com.portfolio.agent.answer.routing.domain;

/** The strength with which a subject may bind the current user goal. */
public enum SubjectBindingRole {
    HINT,
    EXPLICIT,
    DEICTIC,
    RESULT_BOUND
}
