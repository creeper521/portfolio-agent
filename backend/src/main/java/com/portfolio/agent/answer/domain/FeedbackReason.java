package com.portfolio.agent.answer.domain;

public enum FeedbackReason {
    INCORRECT,
    OUTDATED,
    UNCLEAR,
    INCOMPLETE,
    EVIDENCE_INSUFFICIENT,
    WRONG_BOUNDARY,
    UI_CONFUSING,
    OTHER_STRUCTURED
}
