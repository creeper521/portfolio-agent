package com.portfolio.agent.answer.domain;

public enum ConversationModelFailureCode {
    REQUEST_BUILD_FAILED,
    TIMEOUT,
    PROVIDER_ERROR,
    EMPTY_RESPONSE,
    INVALID_RESPONSE,
    DISABLED
}
