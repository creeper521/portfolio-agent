package com.portfolio.agent.answer.domain;

public enum ModelExpressionFailureCode {
    REQUEST_BUILD_FAILED,
    PROVIDER_ERROR,
    TIMEOUT,
    EMPTY_RESPONSE,
    INVALID_RESPONSE,
    DRAFT_REJECTED
}
